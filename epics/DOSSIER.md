# DOSSIER — Hồ sơ dự thi (6 thành phần BTC, PDF 13 mục MẪU 3, Google Drive công khai)

## Mục tiêu
`make dossier` sinh mọi thành phần trừ hai thứ cần con người (quay video, ký giấy): PDF ≤ 20 trang **đúng 13 mục MẪU 3** (tên và thứ tự nguyên văn, khối thông tin đội, khối ký), 2 video ≤ 5 phút, bản kê khai AI từ registry + lockfile + prompt log, link repo public, prompt log ZIP có hash **và** thư mục Google Drive công khai (mục 13). Chạy hai lần: lần 1 bản nộp tuyến trường (29/9, ghi rõ trạng thái và số đo sơ bộ), lần 2 bản cập nhật/chung kết (nếu BTC cho phép — N02).

## Đầu vào
- `docs/competition/BTC-2026-yeu-cau.md` §4 (6 thành phần), §5 (tiêu chí), §6 (13 mục MẪU 3 + bảng ánh xạ idea.md → mục, khối đội 6 trường/thí sinh, khối ký; mục 13 = link Google Drive mở quyền), `docs/template/AI2026_Mau_ho_so.docx` (chứa MẪU 1–3, chỉ dùng MẪU 3 — CF-02).
- `docs/plan.md` §10 (bảng 7 file đầu ra, lịch 2 ngày cuối, quy tắc trung thực) + Errata; `docs/idea.md` §12 (cách tự động hóa, kịch bản 2 video) + Errata; `docs/prompt.md` §5 D6.
- `docs/decisions/E01-brief.md` D11 (`docs/dossier/src/00..13`), D12 (Drive), D20–D21 (prompt log, redaction gate), D25 (`docs/dossier/private/team.yaml` gitignore), §17.5 (LEG-11: PDF nhắc số hiệu văn bản phải có `verified_on` trong `docs/legal/refs.yaml`), §14 (`build_dossier.py`); `docs/prompt-log/README.md` (xuất ZIP, Drive); `scripts/declaration.py`, `scripts/promptlog_export.py` (E01).
- Phát hiện CF-02/C02 (ngân sách trang), CF-03 (cấu trúc Drive 6 thư mục), CF-06 (kê khai trung thực: API thương mại chỉ ở bước sinh dữ liệu, Claude Code ở bước viết mã), CF-08 (video viết cho bản có thật ngày nộp), CF-10/S3 (mục 7–9 bằng chứng thay thế), N09 (PII đội không vào repo), N10 ("phần đội tự xây" = ADR, kịch bản/sư phạm, thiết kế eval, dữ liệu duyệt tay, pilot; 10 câu phản biện), N15 (tag = bản được nộp).
- Kết quả: lần 1 cần E02–E05, E09-lite, E10-lite, E11-lite, E12-lite, PILOT tầng 1; lần 2 cần E06–E08, E10–E12 đủ, PILOT tầng 2.

## Công việc
1. `docs/dossier/src/00-thong-tin-doi.md` (placeholder `{{team:<key>}}` đọc từ `docs/dossier/private/team.yaml`, mẫu `team.example.yaml`; 6 trường/thí sinh; 1–3 thí sinh) và 13 file `01-bai-toan.md`, `02-muc-tieu-pham-vi.md`, `03-du-lieu.md`, `04-tien-xu-ly.md`, `05-mo-hinh.md`, `06-huan-luyen.md`, `07-chi-so-danh-gia.md`, `08-ket-qua.md`, `09-so-sanh.md`, `10-kien-truc-trien-khai.md`, `11-rui-ro-bao-mat.md`, `12-huong-phat-trien.md`, `13-lich-su-cau-lenh.md` (H1 của mỗi file là tên mục MẪU 3 nguyên văn — `build_dossier.py` kiểm); nội dung khung từ idea.md theo bảng ánh xạ BTC §6; số liệu chỉ qua placeholder `{{eval:<key>}}` đọc từ `eval/reports/latest.json` (do `make eval`/`make pilot-report` gom từ `eval/reports/*.md`, `training/reports/ablation.md`, `eval/pilot/report.md`); link Drive qua `{{team:drive_url}}`.
2. Ngân sách trang (CF-02): mục 1–2: 2; 3–4: 2,5; 5–6: 3; 7–9: 5; 10: 2; 11: 2; 12: 1; 13 + khối đội + ký: 1,5 → ≤ 20 trang; mỗi trang ≥ 1 bảng/ảnh/biểu đồ thật.
3. Bằng chứng thay thế cho mục 7–9 ở bản 29/9 (S3/CF-10): eval nhỏ nhưng thật (100 Q&A citation-support, 100 hội thoại, red-team 50–100 = 0, load 10, p95 theo đường), ablation **thành phần** cùng model base (RAG/guardrail/xác nhận ý định bật–tắt — E05), thử nghiệm sơ bộ n = 5–10 (SUS, hoàn thành, thời gian, MOS), URL công khai + status page; mục "kết quả chưa đạt" bắt buộc; mục 12 ghi lộ trình (SFT/DPO, 12 kịch bản, kèm cặp app thật, pilot 30, APK) đủ rõ để bản chung kết vẫn "khớp hồ sơ" (S11).
4. `docs/dossier/build/build_dossier.py`: tách phần MẪU 3 khỏi docx mẫu (xóa MẪU 1–2), điền python-docx, docx → PDF bằng LibreOffice headless theo đúng script hiện có: `find_soffice()` tìm `soffice` trên PATH/thư mục cài mặc định (máy dev, CI có LibreOffice) **hoặc** container LibreOffice (`docker run --rm … soffice --headless --convert-to pdf`, brief D29) trên máy không cài — cả hai đều chấp nhận, không tìm thấy thì in lệnh chạy tay; `pypdf` đếm trang. Cổng non-draft **đã có** trong `build_dossier.py`: (a) số hiệu văn bản pháp luật trong PDF phải khớp mục có `verified_on` trong `docs/legal/refs.yaml` (LEG-11); (b) mọi `{{eval:*}}` đã được điền. **Phải làm ở epic này**: (c) script so khớp số liệu trong PDF ↔ `eval/reports/latest.json` và `eval/pilot/report.md` = 0 sai lệch (chưa có); `DRAFT=1` bỏ qua cổng nhưng in danh sách thiếu.
5. Đầu ra `docs/dossier/out/`: `01_TaiLieuDuAn_CTCV.pdf`, `02_VideoThuyetTrinh.mp4`, `03_VideoDemo.mp4`, `04_XacNhanSV_*.pdf` (quét, người), `05_Repo.txt` (tag, README chạy 1 lệnh, LICENSE Apache-2.0, SECURITY.md; repo public đúng ngày nộp), `06_KeKhaiAI.pdf` (từ `make declaration` → `06_KeKhaiAI.md` → PDF; 3 mục đội tự xây / AI hỗ trợ / kế thừa mã nguồn mở; nêu rõ provider sinh dữ liệu và Claude Code — CF-06/LIC-09), `07_PromptLog.zip` + `.sha256` + `07_PromptLog.zip.REDACTIONS.md` (`make promptlog-export`, redaction gate D21).
6. Google Drive (D12, CF-03): cây `drive/` cố định 7 thư mục, khớp bảng trong `docs/dossier/src/13-lich-su-cau-lenh.md`: `01-prompt-log/` (JSONL gốc + bản Markdown dễ đọc mỗi phiên + INDEX hash + `pre-D1/`), `02-system-prompts/` (snapshot CLAUDE.md, `.claude/agents`, `.claude/skills`, `settings.json`, `config/prompts` theo git hash — `docs/prompt-log/system/`), `03-subagent-transcripts/`, `04-minh-chung-theo-ngay/` (`docs/screens/YYYY-MM-DD/` + snapshot DAILY.md từ `make evidence`), `05-tai-lieu-ky-thuat/` (ADR, README, eval reports), `06-ma-nguon/` (link repo + zip tag), `07-prompt-cong-cu-khac/` (`docs/prompt-log/tools/` — N14), `README.md` trong Drive giải thích các tầng system prompt và quy tắc redaction. **Việc của epic này**: thêm cờ `--drive` cho `scripts/promptlog_export.py` để dựng cây trên vào `docs/dossier/out/drive/` từ bản sao đã redaction + kết quả `make evidence` (E01 mới xuất `07_PromptLog.zip` + `.sha256` + `REDACTIONS.md`, chưa có cây `drive/`); tới khi có, người dùng tạo tay 7 thư mục theo bảng mục 13. Người dùng upload (rclone hoặc tay), mở "Anyone with the link", dán link vào mục 13 và `05_Repo.txt`; Drive **không** chứa `team.yaml`/`private/` hay dữ liệu pilot thô.
7. Video (CF-08): script + slide `docs/dossier/video/` (`thuyet-trinh-5-phut.md`, `demo-5-phut.md`, `slide-outline.md` — khung đã có, cập nhật) viết cho **bản có thật ngày nộp** (không nhắc tính năng chưa có; nêu lộ trình); demo: Playwright ghi màn hình kịch bản chuẩn trên staging 1080p + 1 cảnh người thật (thành viên đóng vai hoặc người thân có phép) + thuyết minh giọng thật/TTS gắn nhãn; ffmpeg dựng, phụ đề; thời lượng ≤ 300 s; không lộ dữ liệu người dùng.
8. `docs/dossier/src/` bổ sung "Giải thích cho người" ≤ 1 trang mỗi mô-đun và 10 câu phản biện kèm câu trả lời (N10; reviewer đọc PDF như giám khảo — prompt.md D6).
9. Checklist nộp (plan §15 cập nhật): tag `v1.0` (lần 1) / `v2.0` (lần 2), CI xanh trên tag, repo public, status page xanh ≥ 24 giờ, 7 file đúng tên, PDF ≤ 20 trang, video ≤ 300 s, ZIP hash khớp INDEX, kê khai khớp lockfile + registry, giấy xác nhận SV đủ thành viên, thông tin đội khớp MẪU 3, link Drive mở được ở chế độ ẩn danh, đã nộp và lưu biên nhận.

## Đầu ra
`docs/dossier/out/` 7 file + `docs/dossier/out/drive/` cây 7 thư mục (sau khi có `--drive`); PDF 13 mục ≤ 20 trang; link Drive công khai trong mục 13; `06_KeKhaiAI.pdf` trung thực; `07_PromptLog.zip` hash khớp; script/slide 2 video; "Giải thích cho người" + 10 câu phản biện; biên nhận nộp (người).

## Test nghiệm thu (tự động, phải xanh)
- `make dossier DRAFT=1` — sinh đủ 7 tên file (video có thể là placeholder ở bản nháp) và in danh sách `{{eval:*}}` còn thiếu; không lỗi python-docx.
- `make dossier` — PDF có đúng 13 mục theo thứ tự nguyên văn (script đọc PDF so với `docs/competition/BTC-2026-yeu-cau.md` §6), ≤ 20 trang (`pypdf`), khối đội + khối ký có mặt, không còn MẪU 1–2; cổng LEG-11 xanh (số hiệu chỉ từ mục có `verified_on`); so khớp số liệu PDF ↔ `eval/reports/latest.json`/`eval/pilot/report.md` = 0 sai lệch (plan §10; cổng (c) hiện thực ở epic này).
- `make declaration` — mọi gói trong `uv.lock`/`apps/web/package.json` có trong bảng; giấy phép model khớp `data/registry/models.yaml` (PhoWhisper BSD-3-Clause, YOLOX Apache-2.0); số phiên prompt log khớp INDEX.
- `make promptlog-export` — hash mọi file khớp `docs/prompt-log/INDEX.md`; gitleaks + regex PII sạch hoặc đã redaction ghi trong INDEX/README (D21); `07_PromptLog.zip.sha256` sinh ra; `make promptlog-export --drive` (sau khi hiện thực ở epic này) dựng đủ 7 thư mục + README vào `docs/dossier/out/drive/`.
- `uv run pytest scripts` — test `check_docs`, `promptlog_export`, `declaration` xanh; `uv run pytest docs/dossier/build` (nếu có test của gói J) — parser MẪU 3, đếm trang, cổng LEG-11 trên fixture.
- Kiểm tra thủ công ghi trong PR (người): 2 video ≤ 300 s (`ffprobe`), link Drive mở được ở cửa sổ ẩn danh, `04_XacNhanSV_*.pdf` đủ thành viên.

## Không được làm
Bịa số hoặc dùng số chưa đo; bỏ mục "kết quả chưa đạt"; dùng dàn ý 10 khối của idea §12/plan §10; đưa `team.yaml`, dữ liệu pilot thô, `.env` lên Drive/repo; sửa tay `docs/prompt-log/**` hay "làm sạch" transcript ngoài redaction gate tự động; trích số hiệu văn bản chưa xác minh; ảnh màn hình không phải của bản được tag nộp (N15); video nhắc tính năng chưa có ở ngày nộp (CF-08); in kèm MẪU 1–2.

## Câu hỏi mở (≤ 3)
1. N02: đội tuyến trường tự upload 6 thành phần hay trường upload; hạn nội bộ của trường; BTC có cho cập nhật hồ sơ trước "kiểm tra hồ sơ" chung kết? (gọi 18–19/9, ghi vào `BTC-2026-yeu-cau.md`).
2. Ai giữ tài khoản Google Drive dùng để công khai (tài khoản đội hay cá nhân) và ai chịu trách nhiệm mở quyền trước khi nộp?
3. Số thí sinh trong khối thông tin đội: 3 hay ít hơn (chỉ người đã cam kết có mặt 10–11/10 và 20–22/11 — N08)?

## Checklist cho người dùng (≤ 30 phút)
- [ ] Đọc PDF nháp như giám khảo: 13 mục đúng tên, mục 8 nói rõ "sơ bộ n = …", mục "kết quả chưa đạt" có thật.
- [ ] Điền `docs/dossier/private/team.yaml` (không commit); kiểm tra khối đội trong PDF.
- [ ] Mở link Drive ở cửa sổ ẩn danh: thấy đủ 7 thư mục và README.
- [ ] Ký `06_KeKhaiAI.pdf` sau khi đối chiếu provider sinh dữ liệu và số phiên prompt log; ký khối "Đại diện đội thi".
- [ ] Nộp đúng cổng/tuyến theo câu trả lời N02; lưu biên nhận vào `docs/dossier/private/`.

## Mốc thực tế
Lần 1 (ADR-006 Pha A): 27/9 (CN) `make dossier DRAFT=1` + quay video; 28/9 (T2) dựng video, `make dossier`, upload Drive, tag `v1.0`; 29/9 (T3) nộp cho trường/BTC theo N02 — **trước** hạn trường gửi danh sách 30/9 (T4); hạn nội bộ của trường có thể sớm hơn → hỏi ngay 18/9. Lần 2 (cập nhật/chung kết, nếu được phép): 10–16/11, tag `v2.0` 17/11 (T3, T-72h). plan §6/§10 ghi D20–D21 (= 7–8/10) — sau hạn 30/9 (CF-01); dàn ý 10 khối thay bằng 13 mục MẪU 3 (CF-02); ZIP thay bằng ZIP + Drive (CF-03).
