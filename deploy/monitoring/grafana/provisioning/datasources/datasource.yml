# Grafana datasource provisioning (prod overlay): the in-stack Prometheus.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: ctcv-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s
