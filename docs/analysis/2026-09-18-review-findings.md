# Phát hiện đã kiểm chứng từ rà soát 3 file (2026-09-18)

Nguồn: docs/analysis/2026-09-18-review-findings.json (đầy đủ). Mỗi mục: id [mức] (lăng kính) lĩnh vực — kết luận → khuyến nghị.

- **CF-01** [blocker] (competition-fit) Lịch thực hiện vs hạn đăng ký/nộp hồ sơ BTC — Toàn bộ kế hoạch 21 ngày đặt mốc nộp hồ sơ ở D20–D21 (≈ 7–8/10/2026 nếu D1 = 18/9) và pilot ở D17–D19 (4–6/10), tức SAU cả hai hạn BTC: đội tự do phải nộp hồ sơ + 2 video + Prompt Log + minh chứng ngay khi đăng ký ≤ 20/9 (còn 2 ngày, chưa có mã), tuyến trường phải có hồ sơ để trường xét và gửi danh sách trước 30/9 (= D13, thực tế hạn nội bộ của trường còn sớm hơn). Không file nào neo D-number vào lịch dương nên /dail… → Chọn tuyến trường (TDTU). Hôm nay: liên hệ Đoàn trường/Phòng CTSV hỏi hạn nội bộ, hình thức xét (thi/xét hồ sơ/phỏng vấn), số suất, ngày đội phải upload hồ sơ lên cổng BTC, và BTC có cho cập nhật hồ sơ trước chung kết không (chung kết có "Kiểm tra hồ sơ, xác minh sản phẩm"); xác nhận 3 thành viên cùng trường, ≤ 22 tuổi, rảnh 10–11/10 và 20–22/11 (không được thay người). Viết lại plan.md mục 2 và 6 thành 3 pha có ngày dương: Pha A "hồ sơ sơ bộ" 18/9–28/9 (D1–D10): E01 → E02 (4–6 kịch bản) → E03 + E04 → E05 (guardrai…
- **CF-02** [major] (competition-fit) Cấu trúc PDF theo MẪU 3 — Dàn ý PDF trong cả ba file là 10 phần chia theo số trang, không phải 13 mục bắt buộc của MẪU 3 (đúng tên, đúng thứ tự) kèm khối thông tin từng thí sinh và khối ký; `make dossier` theo plan.md sẽ sinh "mỗi mục một file" từ dàn ý 10 phần rồi python-docx điền thẳng vào file docx mẫu — nhưng file docx đó chứa liên tiếp MẪU 1 (Bảng A), MẪU 2 (Bảng B) và MẪU 3 (Bảng C), nên PDF sẽ in kèm 2 mẫu thừa nếu không cắt; không có … → Thay dàn ý ở idea.md L332–343 và plan.md L357 bằng 13 file `docs/dossier/src/01-bai-toan.md … 13-lich-su-cau-lenh.md` đặt tên đúng nguyên văn mục MẪU 3, kèm ngân sách trang (gợi ý: mục 1–2: 2 trang; 3–4: 2,5; 5–6: 3; 7–9: 5; 10: 2; 11: 2; 12: 1; 13 + khối đội + ký: 1,5) và bảng ánh xạ idea.md → mục (lấy từ BTC-2026-yeu-cau.md mục 6). Thêm `docs/dossier/team.yaml` (6 trường/thí sinh) và đưa "phân công thành viên" vào mục 5/6 (phần đội tự xây) hoặc Drive. Generator: tách riêng phần MẪU 3 (xóa MẪU 1–2) hoặc dựng docx …
- **CF-03** [major] (competition-fit) Prompt Log và minh chứng (mục 13 MẪU 3, thành phần bắt buộc) — Cả ba file chỉ dự kiến nộp Prompt Log dạng ZIP có hash, trong khi MẪU 3 mục 13 (và điều kiện đăng ký tuyến tự do) đòi một link thư mục Google Drive đã mở quyền truy cập chứa lịch sử câu lệnh, hình ảnh minh chứng "từ bản nháp đến khi hoàn thiện", tài liệu kỹ thuật, mã nguồn; không có bước upload/đặt quyền, không có kế hoạch chụp ảnh minh chứng theo tiến trình; các phiên AI đã tạo ra idea/plan/prompt ngày 17/9 (trước k… → Thêm hai target `make evidence` và `make drive-sync` (rclone hoặc checklist upload tay) tạo cấu trúc Drive cố định: `01-prompt-log/` (JSONL gốc + bản Markdown dễ đọc mỗi phiên + INDEX hash), `02-system-prompts/` (CLAUDE.md, .claude/agents/*.md, .claude/skills/**, config/prompts/* theo phiên bản git), `03-subagent-transcripts/`, `04-minh-chung-theo-ngay/` (mỗi lần /daily chụp 3–5 ảnh app bằng Playwright + snapshot DAILY.md, tên `YYYY-MM-DD-E0X-*.png`), `05-tai-lieu-ky-thuat/` (ADR, README, eval reports), `06-ma-nguo…
- **CF-04** [major] (competition-fit) Độ tin cậy hook prompt log (nguy cơ mất thành phần bắt buộc / trông như bị chỉnh sửa) — promptlog.sh (prompt.md L403–406) phụ thuộc jq và thoát 0 âm thầm khi thiếu transcript_path; vì Stop chạy sau mỗi lượt, tên file theo phút+HEAD bị cp đè và INDEX.md nhận nhiều dòng hash khác nhau cho cùng đường dẫn, nên tiêu chí "hash khớp, không file bị sửa" (plan.md L345) sẽ thất bại; SessionEnd gọi `--close` nhưng script không xử lý tham số, và plan.md L121 mô tả file `.md` có hash commit đầu-cuối trong khi script… → Viết lại hook bằng Python (`.claude/hooks/promptlog.py`, đọc JSON stdin bằng `json`, không cần jq): một file mỗi `session_id` (cập nhật ở Stop, chốt ở SessionEnd), INDEX chỉ giữ một dòng/session với hash cuối; nếu không tìm thấy transcript → exit khác 0 và in lý do ra stderr; SessionStart hook và `make doctor` kiểm tra phiên trước đã có log; commit `docs/prompt-log/` mỗi ngày để git history là bằng chứng toàn vẹn thay cho INDEX tự ghi; ghi kết quả test hook vào ADR-001.
- **CF-05** [major] (competition-fit) Vòng khu vực (60% điểm) — hackathon 2 ngày trên dataset BTC — Kế hoạch dồn toàn bộ D1–D21 cho sản phẩm (40% điểm) và chỉ có một buổi diễn tập 8 giờ không ghi ngày cho vòng khu vực (60%); với hôm nay là 18/9 thì D21 sớm nhất rơi vào 8/10 nên buổi diễn tập chỉ có thể diễn ra ngay sát hoặc trùng hackathon 10–11/10. Bộ kit (idea.md L357–366) chỉ gồm ingestion/RAG/agent/sandbox/giọng nói/eval/deploy, không có EDA, mô hình bảng, geospatial, time-series hay dashboard nhanh cho "datase… → Sau khi nộp hồ sơ sơ bộ, dành trọn 1/10–9/10 cho vòng khu vực: (a) thêm vào plugin ctcv-kit một "ds-kit": template EDA (pandas/polars → báo cáo HTML), baseline tabular (LightGBM/sklearn) + eval + SHAP, pipeline văn bản tiếng Việt (underthesea/PhoBERT/bge-m3), geospatial (geopandas/folium), dashboard Streamlit/Gradio, generator slide + báo cáo theo mẫu BTC; (b) thay lịch 48 giờ bằng hai kịch bản 16 giờ và 20 giờ, phân vai 3 người theo giờ, mốc "phải có demo chạy" ở giờ 10; (c) chế độ CPU-only trên laptop: Qwen3.5-2B…
- **CF-06** [minor] (competition-fit) Bản kê khai công cụ AI/dữ liệu/thư viện (thành phần 6) — tính trung thực và nhất quán — Ba file còn chứa số liệu cũ/sai (idea.md L451 PhoWhisper "Apache-2.0", L15/L109/L153/L452 "YOLO" giấy phép mở; "Qwen3.5-1.7B" ở idea L151/L166/L270, plan L252/L279/L486, prompt L181/L205) nhưng nguồn sự thật thật sự của bản kê khai là data/registry/*.yaml (plan L244, L620), và docs/decisions/E01-brief.md đã chốt D7 (YOLOX Apache-2.0), D8 (Qwen3.5-2B), D10 (PhoWhisper BSD-3-Clause) cùng khởi tạo registry với giấy phép… → Sửa cả ba file trước khi chạy E01: PhoWhisper → BSD-3-Clause; planner_small → Qwen3.5-2B (hoặc 4B nếu RAM điện thoại/laptop cho phép) và cập nhật mọi chỗ APK/ablation; thống nhất YOLOX (Apache-2.0) trong idea.md L15, L109, L153, L452. Trong `data/registry/models.yaml` bắt buộc trường `license` là SPDX id và `license_url`; thêm script `make registry-check` đối chiếu với model card Hugging Face và fail CI nếu lệch; bản kê khai phải nêu rõ API thương mại chỉ dùng ở bước sinh dữ liệu và Claude Code ở bước viết mã, kèm …
- **CF-07** [minor] (competition-fit) Khả năng xác minh sản phẩm trong kỳ chấm hồ sơ sơ bộ và chung kết — Kế hoạch chỉ cam kết URL sống trong 48 giờ đóng băng (plan.md L27, L289–294) và chủ động tắt GPU ngoài giờ pilot/chấm (L307), trong khi ngân sách 300 giờ GPU không đủ chạy liên tục từ ngày nộp (20/9 hoặc 30/9) đến chung kết 22/11; docs chưa có chính sách sẵn sàng rõ ràng cho khoảng chấm hồ sơ online (40%) nếu giám khảo mở link demo trong PDF. Tuy nhiên đây không phải lỗi kiến trúc: idea.md L391 đã nêu "bản mô hình nh… → Thiết kế 2 tầng phục vụ: VPS CPU luôn bật chạy web/API/Postgres/Qdrant + planner nhỏ (Qwen3.5-2B/4B GGUF qua llama.cpp) + PhoWhisper-tiny + TTS cache, đảm bảo URL luôn sống dù chậm hơn; máy GPU bật theo lịch làm "fast path", tự động fallback qua `config/models.yaml` và health check; status page công khai hiển thị chế độ đang chạy; ghi rõ trong PDF mục 10 và video; giữ 300 giờ GPU cho pilot, 30/9–10/10 (chấm hồ sơ) và 18–22/11; thêm vào DoD điều kiện "URL công khai sống liên tục từ ngày nộp đến chung kết".
- **C01** [blocker] (internal-consistency) Lịch trình vs hạn nộp BTC — Lịch 21 ngày (D1 = ngày chạy prompt khởi tạo, sớm nhất 18/9) đặt Pilot ở D17–D19 (≈4–6/10) và Hồ sơ/nộp ở D20–D21 (≈7–8/10), tức sau cả hạn đội tự do 20/9 (phải nộp đủ hồ sơ + 2 video + Prompt Log ngay khi đăng ký) lẫn hạn trường 30/9 (trường còn phải xét nội bộ trước đó), và chỉ còn 1–2 ngày trước hackathon khu vực 10–11/10 nên mốc v1.x 'Diễn tập 8 giờ xong' cũng không có chỗ. idea.md còn giả định pilot diễn ra trướ… → plan.md thắng về quy trình nhưng phải viết lại mục 2 và 6 thành hai pha: Pha A 'Hồ sơ sơ bộ' (≤ hạn nội bộ của Đoàn trường, hỏi ngay hôm nay; giả định ≤ 25/9): E01 → E02 (4 kịch bản) → E03 → E05 tối thiểu (agent có trích dẫn, guardrail cứng) → E14-sơ-bộ (PDF 13 mục theo MẪU 3, 2 video ≤ 5 phút, Prompt Log lên Google Drive, kê khai, giấy xác nhận SV, repo public) + pilot nhỏ 10–15 người (phương án dự phòng idea.md:388) để có số đo thật, ghi rõ quy mô; Pha B (đến 9/10): E04, E06–E13, pilot 30 người, bản v1.0 cho vòng…
- **C02** [major] (internal-consistency) Cấu trúc hồ sơ PDF vs MẪU 3 — Cả idea.md lẫn plan.md định nghĩa dàn ý PDF 20 trang gồm 10 khối theo tiêu chí chấm, và `docs/dossier/src/*.md` 'mỗi mục một file' theo dàn ý đó; nhưng MẪU 3 của BTC bắt buộc đúng 13 mục đánh số với tên nguyên văn, cộng khối thông tin đội và mục 13 là link Google Drive công khai chứa prompt log/ảnh minh chứng. Prompt log trong plan chỉ được nén thành ZIP, không có bước đưa lên Drive và không có chỗ ghi link vào PDF. → BTC đè lên cả hai file: thay dàn ý ở idea.md:332-343 và plan.md:357 bằng bảng 13 mục MẪU 3 (dùng bảng ánh xạ có sẵn ở BTC-2026-yeu-cau.md §6, ghi số trang mục tiêu cho từng mục: 1–2 ≈ 3 trang, 3–4 ≈ 3, 5–6 ≈ 3, 7–9 ≈ 4, 10 ≈ 2, 11 ≈ 2, 12 ≈ 1, 13 ≈ 1 + khối thông tin đội); đổi `docs/dossier/src/` thành 13 file `01-bai-toan.md … 13-lich-su-cau-lenh.md`; thêm vào plan.md:345 và checklist plan.md:626-632 bước 'tải docs/prompt-log/ + docs/screens/ + tài liệu kỹ thuật lên Google Drive, mở quyền Anyone-with-link, dán lin…
- **C03** [major] (internal-consistency) Tên model — Cả ba file dùng 'Qwen3.5-1.7B' cho bản APK offline, môi trường dev và ablation; kích thước này không tồn tại trong dòng Qwen3.5 (chỉ có 0.8B/2B/4B/9B; 1.7B là Qwen3). Claude Code sẽ không tải được model ở E07/E12 và ablation '9B vs 1.7B' không chạy được. Ngoài ra Qwen3.5 vốn đa phương thức nên việc giữ thêm Qwen3-VL-8B trên cùng GPU 24 GB cần được biện minh trong ADR-002. → idea.md thắng về sản phẩm nhưng phải sửa dữ kiện: đổi mọi chỗ '1.7B' thành 'Qwen3.5-2B' (GGUF Q4_K_M ≈ 1,3–1,6 GB, vừa điện thoại 8 GB RAM; 4B là phương án nếu độ trễ < 4 s vẫn đạt), ablation thành '9B máy chủ vs 2B tại chỗ'; cập nhật plan.md:252, :279, :486 và prompt.md:181, :205; trong ADR-002 (prompt.md:144) yêu cầu architect đánh giá phương án 'Qwen3.5-9B làm cả planner và VLM' so với 'Qwen3.5-9B + Qwen3-VL-8B' theo VRAM và độ trễ, và ghi lý do chọn.
- **C04** [minor] (internal-consistency) YOLO vs YOLOX và giấy phép mã nguồn — idea.md dùng "YOLO" chung chung ở 14 chỗ (15, 92, 109, 136, 153, 166, 177, 219, 270, 302, 390, 411, 415, 452) và bảng kê khai dòng 452 ghi giấy phép "Theo giấy phép của bản dùng", trong khi plan.md (158, 214, 253, 269, 482, 488, 596), prompt.md (39, 92, 144, 181, 205) và docs/decisions/E01-brief.md D7 đã chốt YOLOX-Tiny (Apache-2.0) và cấm Ultralytics (AGPL-3.0). Đây là lệch câu chữ trong idea.md (nguồn cho hồ sơ PDF… → plan.md/prompt.md thắng (đây là quyết định stack đã có lý do giấy phép): sửa tất cả 11 chỗ trong idea.md thành 'YOLOX-Tiny (Apache-2.0)', dòng 452 thành 'YOLOX-Tiny | Apache-2.0 | Phát hiện phần tử | Huấn luyện toàn bộ'; thêm vào CLAUDE.md mục Stack (plan.md:480-483) câu 'Cấm ultralytics/AGPL và mọi thư viện copyleft mạnh; pip-audit + script kiểm tra SPDX trong make check'.
- **C05** [minor] (internal-consistency) Bản kê khai: giấy phép và số model tự fine-tune — idea.md:451 vẫn ghi PhoWhisper là Apache-2.0 (thật là BSD-3-Clause) và idea.md:145 tuyên bố "tự fine-tune 3 mô hình" gồm ASR trong khi ASR LoRA chỉ là "tùy chọn" (idea.md:149, plan.md:254) và bị cắt đầu tiên ở D17 (plan.md:376); tuy nhiên giấy phép đã được sửa trong registry theo docs/decisions/E01-brief.md D10 nên bản kê khai sinh tự động sẽ đúng, chỉ còn cần sửa văn bản idea.md để không mâu thuẫn nội bộ và không qu… → Sửa idea.md:451 thành 'BSD-3-Clause (VinAI)'; sửa idea.md:145 thành 'tự fine-tune 2 (huấn luyện viên, bộ phát hiện giao diện); ASR LoRA tùy chọn nếu có ≥ 5 giờ giọng pilot có đồng thuận'. Thêm vào `make data` bước 9 (plan.md:244) một kiểm tra: trường `license` trong data/registry/models.yaml phải khớp SPDX đọc từ model card Hugging Face (không cho phép giá trị tự điền), và test schema thất bại nếu license = 'unknown'.
- **C06** [major] (internal-consistency) Cây thư mục định nghĩa 3 nơi khác nhau — idea.md 15.1 (được prompt.md:144-145/:472 và plan.md:407 coi là cây chuẩn, skill ctcv-conventions cấm tạo thư mục ngoài nó) thiếu config/, drills/, tests/, hackathon/, deploy/models, docs/{decisions,status,ops,screens,dossier/src,dossier/out}, training/{runs,reports,budget.json}, eval/{sets,reports,redteam,pilot}, .claude/, .mcp.json, .claude-plugin/ mà plan.md và prompt.md dùng như đã có; prompt.md:145 liệt kê lại m… → Giữ idea.md 15.1 là nơi duy nhất định nghĩa cây (idea thắng về sản phẩm, các file khác chỉ tham chiếu) và cập nhật nó thành cây đầy đủ: thêm `config/{models,tools,guardrail,eval}.yaml`, `config/prompts/`, `drills/scenarios/`, `sandbox/engine/` (Python, được coverage) + `sandbox/scenarios/`, `tests/{unit,integration,invariants}/`, `hackathon/input/`, `deploy/models/`, `docs/{competition,template,decisions,status,ops,screens,dossier/src,dossier/out,prompt-log}`, `training/{runs,reports,budget.json}`, `eval/{sets,repo…
- **C07** [major] (internal-consistency) Prompt log: định dạng, tên file, nội dung và lỗi hook — plan.md quy định một file `.md` mỗi phiên tên `YYYY-MM-DD-HHMM-E0X.md` chứa system prompt + hội thoại + lệnh + hash commit đầu-cuối; prompt.md lại ghi `YYYY-MM-DD-HHMM-<shortsha>.jsonl` bằng cách `cp` transcript thô (không có system prompt/CLAUDE.md, không có mã epic). Hook được gắn vào cả Stop (chạy sau MỖI lượt trả lời) và SessionEnd, nên mỗi phiên sinh hàng chục bản sao; hai lượt trong cùng một phút ghi đè cùng tê… → plan.md thắng về quy trình nhưng cần chốt định dạng kỹ thuật một lần trong plan.md §3 và prompt.md §9.3 cùng nội dung: một thư mục mỗi phiên `docs/prompt-log/<YYYY-MM-DD-HHMM>-<session_id>-<E0X>/` (E0X lấy từ tên nhánh git) chứa `transcript.jsonl` (copy nguyên), `system-snapshot/` (CLAUDE.md, .claude/agents, .claude/skills, config/prompts tại thời điểm đó), `commands.log`, `meta.json` (HEAD lúc bắt đầu/kết thúc, model, effort). Stop chỉ ghi đè bản nháp cùng thư mục (idempotent, không đụng INDEX); SessionEnd mới tín…
- **C08** [major] (internal-consistency) Hai bộ prompt bootstrap và nguồn sự thật — plan.md §12 khẳng định P0–P6 là 'toàn bộ những gì bạn gõ' và P0 sinh CLAUDE.md theo Phụ lục A, còn prompt.md §4–6 định nghĩa một prompt tổng khác cho E01 (architect + 5 ADR + reviewer/security + 50 kịch bản red-team + PR '[E01] …') và bộ lệnh /daily, /epic, /review…; hai bộ khác nhau về đầu ra E01, tên PR và thời điểm có make redteam. prompt.md yêu cầu CLAUDE.md có mục 'Delegation' nhưng Phụ lục A không có; prompt.md… → Nâng docs/prompt.md (mới hơn, 18/9) thành nguồn sự thật thứ ba về 'cách vận hành Claude Code': sửa plan.md:7, :138 (thêm bước chép .claude/ và .mcp.json), :463 và prompt.md:135 thành 'ba nguồn sự thật: idea (sản phẩm) > plan (quy trình, kiến trúc) > prompt (vận hành Claude Code)'. Thay toàn bộ plan.md §12 bằng một đoạn 'Bộ prompt: xem docs/prompt.md §4–6' (xóa P0–P6 để tránh hai phiên bản). Bổ sung vào Phụ lục A hai mục '## Delegation' (6 quy tắc prompt.md:67-73) và '## Subagent, skill, hook' (tham chiếu prompt.md …
- **C09** [minor] (internal-consistency) Số epic và epic Pilot/Hồ sơ — Ba file nói "12 epic" và mọi cơ chế thực thi (P1 "Thực hiện epics/E0X.md", skill `/epic` với argument-hint "[E01..E12]", prompt.md:146 "Sinh 12 file") chỉ phủ E01–E12, trong khi bảng lộ trình plan.md:219-220 còn hai hàng Pilot (D17–D19) và Hồ sơ (D20–D21) không có file epic nên không có vòng lặp chuẩn để chạy 5 ngày cuối. Tuy nhiên docs/decisions/E01-brief.md (dòng 36, 184) đã chốt thêm `epics/PILOT.md` và `epics/DOS… → plan.md thắng về quy trình: thêm E13 Pilot và E14 Hồ sơ (file `epics/E13.md`, `epics/E14.md` theo mẫu Phụ lục B; E14 có hai lần chạy 'sơ bộ' và 'v1.0' theo C01), đổi '12 epic' → '14 epic' tại idea.md:16, :291, :405; plan.md:207, :406; prompt.md:146, :166; argument-hint '[E01..E14]'; sửa BTC-2026-yeu-cau.md:3 thành `epics/E14.md`. Cột 'Có từ epic' ở plan.md:601-602 ghi E13/E14.
- **C10** [major] (internal-consistency) Môi trường dev Windows vs hook bash/jq và make doctor — prompt.md viết toàn bộ hook bằng bash + jq + sha256sum + `date -Iseconds`, trong khi máy chạy Claude Code là Windows 11/Git Bash không có jq sẵn (đang cài) và không có GPU; plan.md mô tả 'phần cứng chuẩn' là Ubuntu 22.04 + CUDA và bắt `make doctor` phải thấy 'Docker + GPU nhìn thấy nhau' xanh mới được bắt đầu E02 → trên máy dev điều kiện này đỏ vĩnh viễn và E02 bị chặn theo đúng luật trong plan. → prompt.md §3/§9: viết hook bằng Python 3.12 (`python .claude/hooks/guard.py` với json/hashlib/subprocess; không phụ thuộc jq/sha256sum), cập nhật settings.json command tương ứng và ghi rõ 'hook phải chạy được trên Windows Git Bash và Linux CI'. plan.md §4: tách 'máy dev = Windows 11 (Claude Code, Docker Desktop, không GPU)' và 'máy GPU thuê = Ubuntu 22.04'; tách `make doctor` thành `doctor-dev` (Docker, uv, pnpm, make, jq, 3 file nguồn sự thật, template .docx, .env) và `doctor-gpu` (chạy qua SSH: nvidia-smi, vLLM, …
- **C11** [major] (internal-consistency) Ai sinh 12 kịch bản sandbox và khi nào — E02 (D2–D3) giao '4 kịch bản' và E10 (D13) giao '8 kịch bản mới', nhưng `make data` bước 4 lại 'Sinh kịch bản → sandbox/scenarios/*.json (12 × 3 mức)' tự động bằng LLM ngay từ E03/E06; E06 (D8) cần '5 persona × 12 kịch bản' và E08 (D11) cần render 20.000 ảnh 'mọi trạng thái' của 12 kịch bản trong khi theo thứ tự phụ thuộc E08 chạy song song với E10 và chỉ có 4 kịch bản trước D13. Ngoài ra '12 kịch bản' và '12 × 3 mức… → idea.md thắng về sản phẩm (kịch bản do pipeline sinh, người chỉ duyệt): sửa plan.md §6 thành E02 = engine + validator + 4 kịch bản mức 1 viết tay làm few-shot; E03 (hoặc E06) = `make data` bước 4 sinh đủ 12 × 3 mức = 36 file, id `<slug>-l<level>`, duyệt 30 mẫu; E10 = UI/e2e/dashboard cho 8 kịch bản đã sinh (không 'viết' kịch bản). Đổi thứ tự phụ thuộc plan.md:222 thành 'E06 (đã có 36 kịch bản) → E08'; quy ước đếm: 'kịch bản' = 12 luồng, 'file' = 36; e2e bắt buộc 12 × mức 1, mức 2–3 chạy hằng đêm. Cập nhật idea.md:4…
- **C12** [major] (internal-consistency) Hook agent ở Stop chặn kết thúc mọi lượt — prompt.md:121 và settings.json ở §9.1 (dòng 375-378) gắn hook `type: agent` vào Stop không có matcher/cờ điều kiện, nên nó chạy sau MỌI lượt trả lời (kể cả /daily, plan mode, lượt hỏi ≤3 câu rồi dừng theo plan.md:88-90/416, lượt báo "quá 3 vòng"), mâu thuẫn với vòng lặp hỏi–chờ của plan.md và với chính bảng hook prompt.md:117 (Stop chỉ "nhắc checklist PR", không chặn); docs/decisions/E01-brief.md D4 đã quyết định tha… → prompt.md tự sửa (plan.md thắng về quy trình hỏi–chờ): bỏ agent hook khỏi Stop hoặc gate nó bằng cờ: skill /epic tạo `docs/status/EPIC_ACTIVE` khi bắt đầu bước thực thi và xóa khi PR đã mở; hook Python kiểm tra cờ, chỉ chạy khi cờ tồn tại và transcript không kết thúc bằng câu hỏi cho người dùng; luôn trả 'ok' trong plan mode. Ghi rõ trong prompt.md §3 rằng Stop chạy mỗi lượt, không phải mỗi phiên.
- **C13** [major] (internal-consistency) Ngưỡng và thiết kế pilot lệch giữa idea và plan — idea.md:219 đặt một ngưỡng '≥ 90%' chung cho 1.500 ảnh sandbox lẫn 200 ảnh app thật trong khi plan.md:214 và :264 tách 90%/80%; idea.md:225–228 thiết kế pilot 2 tuần với buổi 2 cách 3–5 ngày, ôn ngày 2/5/12 (idea.md:125) và KPI 'quay lại ngày 5' (idea.md:223), nhưng plan.md cố định pilot D17–D19 ở 5 chỗ (:58, :139, :219, :309, :319–321) với buổi 2 chỉ cách 2 ngày, nên bài ôn ngày 5/12 và KPI quay lại ngày 5 không thể… → Ngưỡng ảnh: plan.md thắng (thực tế hơn) → sửa idea.md:219 thành '≥ 90% sandbox / ≥ 80% app thật; mAP50 ≥ 0,85'. Pilot: idea.md thắng về thiết kế thí nghiệm → sửa plan.md §9 thành buổi 2 = buổi 1 + 5 ngày (khớp KPI 'quay lại ngày 5' và bài ôn ngày 2, 5), tổng cửa sổ pilot 6 ngày; cập nhật E13 theo C01. Cắt phạm vi: thống nhất hai bậc ở cả plan.md:48 và :376: D10 bỏ DPO + kịch bản 10–12; D17 bỏ APK offline + LoRA giọng + kịch bản 7–9 (giữ tối thiểu 6 theo idea.md:396).
- **F01** [blocker] (technical-feasibility) Lịch trình / trình tự epic — Với D1 = 18/9, plan đặt 'Hồ sơ' ở D20–D21 (= 7–8/10) và xếp fine-tune, VLM, APK, pilot 30 người trước hồ sơ, trong khi hồ sơ sơ bộ (chiếm 40% điểm khu vực) phải nộp trước 20/9 (đội tự do) hoặc đi kèm danh sách đề cử của trường trước 30/9 (= D13); vòng khu vực miền Nam 10–11/10 = D23–D24 và buổi tập dượt 8 giờ không có ngày nào trong lịch. Kế hoạch theo thứ tự hiện tại không thể nộp đúng hạn, và Claude Code sẽ sinh 12… → Sửa bảng mục 6 plan.md và mục 11 idea.md TRƯỚC khi chạy P0: thêm mốc 'v0.2 Hồ sơ sơ bộ' hạn D11 (28/9) gồm E01 → E02 (6 kịch bản) → E03 → E04 → E05 (base model + prompt có cấu trúc, chưa fine-tune) → E09-lite (10 drill) → E11-lite (50 red-team, load test nhẹ) → deploy staging công khai → mini-pilot 10–15 người D9–D10 → `make dossier` D10–D11 với mục 'kết quả sơ bộ'. E06/E07/E08/E10/E12 dời sang D12–D21 hướng tới vòng khu vực và chung kết (chung kết có 'kiểm tra hồ sơ, xác minh sản phẩm' để cập nhật số liệu). Dành D…
- **F02** [minor] (technical-feasibility) Model — Qwen3.5-1.7B không tồn tại (dải nhỏ Qwen3.5 là 0.8B/2B/4B/9B); tên này xuất hiện đúng 9 chỗ trong 3 file (idea.md L151/L166/L270, plan.md L252/L269/L279/L486, prompt.md L181/L205) và phải đổi thành Qwen3.5-2B (đã được chốt ở docs/decisions/E01-brief.md D8 nhưng 3 file gốc chưa cập nhật), nếu không APK offline và ablation 9B vs bản nhỏ sẽ hụt model. → Thay cả 9 chỗ bằng `Qwen3.5-2B` nếu vẫn giữ bản chạy tại chỗ, hoặc xóa hẳn `planner_small` và bước GGUF theo F09. Nếu giữ, ablation phải so cùng điều kiện: train thêm LoRA cho 2B (~40 phút, cùng dữ liệu) hoặc ghi rõ '2B base, chưa fine-tune'.
- **F03** [major] (technical-feasibility) VRAM / serving — Qwen3-VL-8B riêng là thừa vì mọi cỡ Qwen3.5 đã đa phương thức (vision) natively; hai instance vLLM (9B AWQ + VL-8B) cùng BGE-m3, reranker, ASR, TTS trên một GPU 24 GB không còn chỗ cho KV cache của '50 người đồng thời'; 'VLM tải theo yêu cầu' không khả thi vì vLLM không hot-swap model (nạp lại 8B mất 1–2 phút mỗi lần gọi kèm cặp). → Bỏ Qwen3-VL-8B khỏi ADR-002, `config/models.yaml`, compose, kê khai (idea.md L450). `/coach/screen` gọi cùng instance Qwen3.5-9B với `--limit-mm-per-prompt image=1`, `--max-model-len 4096`. LoRA chỉ nhắm projection của phần ngôn ngữ, merge vào bf16 rồi mới lượng tử hóa để vLLM phục vụ đúng một model. Dùng VRAM dư cho ASR trên GPU (F05, F07). Đổi dòng ablation 'VLM đơn vs YOLOX+VLM' thành 'VLM có/không JSON schema + grounding'.
- **F04** [major] (technical-feasibility) Fine-tune — Cấu hình 'bf16 LoRA, seq 4k, Qwen3.5-9B' sẽ OOM trên 24 GB ở batch 1; hỗ trợ của PEFT/bitsandbytes/AutoAWQ/llama.cpp cho kiến trúc hybrid Gated-DeltaNet/MoE của Qwen3.5 chưa được kiểm chứng trong plan; và cùng một GPU vừa train (D9–D10) vừa phục vụ staging mà E08–E10 và checklist người dùng cần — '1 đêm' không có ngân sách cho lần chạy hỏng đầu tiên. → (1) QLoRA nf4, hoặc bf16 LoRA với seq ≤ 2.048 + packing + flash-attn + chunked cross-entropy (hội thoại huấn luyện viên ≤ 2 câu/lượt nên 4k là thừa); 2 epoch với early stopping đã có. (2) Thêm vào `make doctor` (E01, ngày 1) một 'toolchain smoke' trên máy GPU: tải Qwen3.5-9B, 10 bước LoRA, lượng tử hóa (hoặc dùng checkpoint FP8/AWQ chính thức của Qwen nếu có), `vllm serve` model đã merge, và `convert_hf_to_gguf` chỉ nếu còn giữ offline. Ghi ma trận dự phòng vào ADR-002: AWQ lỗi → FP8 trên RTX 4090 (chốt 4090, bỏ A1…
- **F05** [major] (technical-feasibility) Độ trễ / load test — p95 < 2,5 s đầu-cuối với 50 người đồng thời không đạt được với chuỗi ASR → planner → tool → sinh câu → verify (reranker + LLM-judge) → TTS trên một GPU 24 GB; riêng ASR trên 4 vCPU đã tiêu 1,5–3 s cho 8 s audio; 'p95 một lượt < 1,2 s' cho agent hai bước cũng không đạt ở tải 50 in-flight. Đây là DoD #4 nên `make loadtest` sẽ đỏ vĩnh viễn. → Tách mục tiêu theo đường đi: (a) bước sandbox — engine máy trạng thái quyết định bước kế, câu huấn luyện viên sinh sẵn theo (scenario, screen, mistake) và cache TTS ⇒ p95 < 1 s, không gọi LLM trực tuyến; (b) hỏi đáp/kèm cặp — p95 < 4 s, thời gian tới âm thanh đầu tiên < 1,5 s nhờ SSE + TTS theo câu. Bỏ LLM-judge khỏi verify đồng bộ (chỉ điểm reranker; judge chạy offline trong `make eval`). Phân loại ý định bằng rule/embedding, không gọi LLM. ASR chạy trên GPU. Định nghĩa load test = 50 phiên hoạt động với think-tim…
- **F06** [major] (technical-feasibility) TTS — Hai ứng viên TTS đều có vấn đề chưa được nêu: F5-TTS (flow-matching) tốn 1–3 s/câu trên GPU chia sẻ và checkpoint gốc cùng đa số bản Việt phát hành CC-BY-NC-4.0 (xung đột với 'Tỉnh đoàn tự host' + Apache-2.0); Piper nhanh và MIT nhưng giọng máy móc, khó đạt 'MOS ≥ 4/5' với 10 người cao tuổi. ADR-002 để mở lựa chọn mà không có tiêu chí, và E04 không có test nghiệm thu TTS nào. → Ghi tiêu chí chọn vào plan.md mục 7 / ADR-002: giấy phép MIT/Apache/BSD cho cả code và checkpoint, RTF trên CPU < 0,3, có giọng tiếng Việt sẵn; mặc định Piper vi_VN + stream theo câu + cache ~500 câu lặp; chỉ cân nhắc F5/VietTTS sau khi đọc thẻ model. Đổi mục tiêu MOS thành '≥ 3,5, báo cáo trung thực'. Thêm vào E04: 'TTS 20 câu mẫu, RTF < 0,3, gắn nhãn mô phỏng'.
- **F07** [major] (technical-feasibility) ASR / eval — Tập đánh giá WER không đo được điều muốn đo: Common Voice vi hầu như không có metadata giọng vùng miền và rất ít người > 60 tuổi, VIVOS toàn giọng Nam/người trẻ/đọc sạch, còn audio TTS là dữ liệu tổng hợp sạch — nên 'WER theo giọng Bắc/Trung/Nam và nhóm tuổi' không tính được từ nguồn này. PhoWhisper-small đã ở khoảng 10–13% WER trên Common Voice (đọc, sạch) theo bảng của VinAI, nên '≤ 18% giọng địa phương' cho người … → Tự ghi 60–90 phút audio (3 thành viên + người thân 50+, 3 miền, câu lệnh thực tế của sản phẩm, phiếu đồng thuận, phát hành CC-BY) làm tập 'giọng vùng miền / người cao tuổi'; Common Voice/VIVOS chỉ làm tập 'giọng chuẩn'; bỏ TTS khỏi tập WER. Dùng PhoWhisper-medium INT8 trên GPU (VRAM dư sau F03; 8 s audio ≈ 0,5 s trên 4090). WER là chỉ số báo cáo theo lát cắt trong hồ sơ, không phải cổng chặn trong `make check`; giữ 'nói lại' + bàn phím chữ to là hành vi mặc định khi confidence thấp.
- **F08** [major] (technical-feasibility) Vision / YOLOX — YOLOX huấn luyện thuần trên 20.000 ảnh render từ sandbox React của đội (idea L153/L177, plan L240/L253) nhưng mô-đun kèm cặp lại nhận ảnh app thật (idea L83/L92); không tài liệu nào bàn khoảng cách sim-to-real, mAP50 chỉ đo trên ảnh sandbox giữ lại còn cổng '≥ 80% trên 200 ảnh app thật' không có nguồn thu thập, không có trong bảng dữ liệu (L177) hay registry, chỉ ghi 'chụp 10 màn hình' (plan L214); 'che số nhạy cảm b… → Bỏ YOLOX khỏi v1.0 (ghi BACKLOG.md kèm lý do); dùng grounding của Qwen3.5 (vision native) trả JSON {screen, state, next_element_bbox, pii_bboxes}; render ~3.000 ảnh (6 viewport) chỉ để eval VLM; chụp 100 ảnh app thật từ máy của 3 thành viên, che tay bằng script trước khi đưa vào repo/eval. Che PII = làm mờ bbox do VLM trả về trước khi lưu log; ADR-005 nói rõ 'model tự host nên che trước khi lưu, không phải trước khi vào model'. E08 còn: prompt VLM + schema + overlay UI + TTL, vừa 1–2 ngày.
- **F09** [major] (technical-feasibility) APK offline — APK offline (GGUF + PhoWhisper-tiny + ONNX + build Android) nằm trong phạm vi mặc định và bị xếp vào D15–D16 cùng deploy prod/monitoring/backup/failover (plan.md L44, L218; prompt.md L205), trên máy dev Windows chưa có Android SDK/NDK và llama.cpp chưa xác minh hỗ trợ kiến trúc Gated-DeltaNet của Qwen3.5; quy tắc cắt phạm vi chỉ kích hoạt ở D17 (plan.md L376) — tức sau khi hai ngày APK đã tiêu tốn — nên không giảm đư… → Thay 'APK offline' bằng 'PWA offline': service worker cache toàn bộ kịch bản JSON + câu huấn luyện viên sinh sẵn theo (scenario, screen, mistake) + clip TTS sinh sẵn; hỏi đáp/kèm cặp hiện thông báo 'cần mạng'. Xóa `apps/android`, `planner_small`, bước GGUF, dòng ablation '9B vs tại chỗ' và dòng 'Bản offline' ở idea.md L270; giữ APK trong 'Hướng phát triển'. Cắt ngay trong plan.md mục 2 thay vì chờ rà D17.
- **F10** [major] (technical-feasibility) CI / cổng chất lượng — `make check` (plan.md L15, L106–L117) gộp `make eval --quick` (LLM-judge, 100 mẫu) và `make redteam` (200 kịch bản chạy trên agent + model thật, vì L273 nói rõ chỉ tầng eval mới gọi model thật) vào cổng bắt buộc trên GitHub Actions (L129 branch protection, L407, prompt.md L145, L212 "chạy trong CI", L331), nhưng không tài liệu nào nêu self-hosted runner, tunnel/secret tới máy GPU staging hay cách CI GitHub-hosted (kh… → Hai tầng rõ ràng: `make check` (tất định, < 10 phút: ruff/eslint, unit, integration với cassette có khóa = sha(prompt version + model name + request), e2e với agent stub, build chỉ web/api/agent/speech bản CPU-slim) và `make gate` (cần staging GPU: `eval --quick`, `redteam`, `loadtest`; chạy từ máy GPU hoặc self-hosted runner trước merge/tag). `models` dùng image vLLM upstream (pull, không build, không tính vào 8 GB). Sửa plan.md L15/L110–L117, prompt.md L69, CLAUDE.md 'Lệnh chuẩn' và DoD trong Phụ lục B.
- **F11** [major] (technical-feasibility) Môi trường dev / portability — Ba tài liệu không có một dòng nào nhắc Windows/Git Bash dù Claude Code chạy trên Windows 11 không GPU: plan.md L142 yêu cầu E01 "cài tự động" Ubuntu 22.04 + NVIDIA/CUDA, L146 và L591 bắt `make doctor` phải thấy "Docker + GPU nhìn thấy nhau" và "tất cả xanh mới bắt đầu E02" (sẽ đỏ vĩnh viễn trên máy dev); L279 nói dev "gọi máy GPU" nhưng không có compose profile cpu/gpu hay cơ chế trỏ `VLLM_BASE_URL` sang máy thuê, nê… → Viết hook bằng Python stdlib (`python .claude/hooks/guard.py`, json/hashlib/datetime) và gọi từ settings.json; promptlog ghi một lần ở SessionEnd theo session_id (Stop chỉ append hash vào INDEX) để không nhân bản transcript mỗi lượt. Compose profiles `gpu` (models, vision) / `cpu`; dev Windows chạy profile cpu và trỏ `VLLM_BASE_URL` sang máy GPU qua SSH tunnel/Tailscale. docx→pdf bằng container LibreOffice (`docker run --rm -v ... soffice --headless --convert-to pdf`) + `pypdf` đếm trang. Bỏ pin torch/Docker/Ubuntu…
- **F12** [major] (technical-feasibility) Dữ liệu / crawl — Kế hoạch crawl E03 (D4, 1 ngày) chỉ xử lý HTML tĩnh (`data/raw/guides/*.html` → Markdown, plan.md L236–237) trong khi nguồn kê tại idea.md L174 gồm SPA/WAF (dichvucong.gov.vn trả shell ~5 KB; vneid.gov.vn "Request Rejected"; thuedientu.gdt.gov.vn không có robots.txt) và tài liệu PDF; không có bước render SPA/trích PDF và không có rủi ro "không gom đủ nguồn" trong bảng rủi ro (chỉ có "hash đổi"). Số ≥ 1.500 trang là "… → Đổi cổng E03 thành: `data/sources.yaml` 50–150 URL/PDF do đội duyệt tay (Playwright render cho SPA, trích PDF bằng pymupdf), ≥ 300 chunk theo bước có cơ quan ban hành/ngày; giữ cổng recall@5 ≥ 0,9 (L238) và 300 Q&A làm tiêu chí thật. Ghi trong kê khai: nội dung chỉ để truy xuất/trích dẫn, snapshot có sha256, không phân phối lại.
- **F13** [major] (technical-feasibility) Phạm vi hạ tầng / eval — Hạ tầng và bộ eval được kê ở quy mô đội lớn (Prometheus+Grafana ngoài Uptime Kuma, MinIO chỉ để giữ ảnh TTL 60 s, testcontainers + k6/Locust + axe + Lighthouse ≥ 90 + 3 mock bố cục, ablation 7 dòng, tập eval 500 hội thoại/1.500+200 ảnh/10 giờ audio/200 red-team, 100 mẫu người chấm hiệu chỉnh judge, MOS 10 người chấm, restore-drill hằng tuần) trong khi quy tắc cắt phạm vi ở plan.md L48 và idea.md L396 chỉ cắt tính năn… → Cắt cho v1.0 và ghi vào BACKLOG.md: Prometheus/Grafana (giữ Uptime Kuma + `/metrics` của vLLM + 1 script p95 từ log JSON), MinIO (Redis SETEX 60 s cho ảnh), testcontainers (dùng compose test), chọn một công cụ load (k6), Lighthouse → 'báo cáo', 3 mock → 1; ablation còn 3 dòng (base / SFT / SFT+DPO; có/không trích dẫn); tập eval: 200 hội thoại, 300 ảnh sandbox + 100 thật, 1 giờ audio, 100 red-team (mở rộng 200 trước chung kết); 100 mẫu người chấm → 50 chỉ cho citation-support; bỏ ASR LoRA và restore-drill hằng tuần …
- **SEC-01** [major] (security-legal-ethics) security/privacy — prompt log & minh chứng công khai — Pipeline prompt log (hook cp nguyên transcript, prompt.md L403–405; ZIP không redaction, plan.md L345) không có bước lọc bí mật/PII trước khi công bố, guard.sh chỉ chặn cat/less/head/tail/grep .env chứ không chặn `docker compose config`/`env`/`printenv`, và plan.md mục 10 hoàn toàn thiếu bước tạo thư mục Google Drive công khai cho MẪU 3 mục 13; tuy nhiên gitleaks đã có trong pre-commit và `make check` (phát hiện nhưn… → (1) Ngăn bí mật vào transcript từ gốc: `make doctor` và mọi script chỉ in TÊN biến + set/unset, không in giá trị; guard chặn `docker compose config`, `env`, `printenv`, `set`, mọi lệnh đọc `.env*` (cat/sed/awk/python open). (2) Thêm cổng trong `make dossier`: chạy gitleaks + regex PII trên ZIP prompt-log và toàn bộ thư mục sẽ đẩy lên Drive; nếu phát hiện, redaction TỰ ĐỘNG theo quy tắc công khai (thay bằng `[REDACTED-SECRET sha256:xxxx]`), giữ hash bản gốc trong INDEX.md và ghi rõ quy tắc này trong bản kê khai để k…
- **SEC-02** [major] (security-legal-ethics) security — hook/permission Claude Code — Hook được tuyên bố là "luật cứng" (prompt.md L104) nhưng guard.sh/promptlog.sh (L388–389, L403) phụ thuộc jq và fail-open (jq thiếu/lỗi ⇒ exit 0 im lặng), nên trên máy dev Windows/Git Bash chưa có jq thì guard và prompt log — thành phần hồ sơ bắt buộc — tự tắt mà không cảnh báo; hai biến env L346–347 không tồn tại; regex L390/L392 và allow-list `Bash(make *)`/`Bash(uv *)`/`Bash(git commit *)` (cho phép `--no-verify`)… → Viết hook bằng Python (`.claude/hooks/*.py`, dùng Python 3.12 sẵn có, `json` chuẩn) — không phụ thuộc jq; fail-closed: mọi lỗi parse ⇒ exit 2 kèm stderr. Guard dùng `shlex` tách token + danh sách mẫu mở rộng (rm với -r/-f mọi thứ tự; git clean/reset --hard/push -f|--force|--force-with-lease; `--no-verify`; docker compose config; env/printenv/set; mọi đường dẫn khớp `(^|/)\.env(\.|$)`). Thêm hook SessionStart tự kiểm: hook chạy được, `docs/prompt-log/` ghi được, python OK — nếu không ⇒ in cảnh báo đỏ vào ngữ cảnh và…
- **SEC-03** [minor] (security-legal-ethics) security — kiến trúc CaMeL planner/quarantine — Tài liệu đã nêu nguyên tắc CaMeL nhưng chưa đặc tả schema kín: idea.md L118 và plan.md L536 đã đưa cả "nội dung web/trang web" qua quarantined LLM (không phải chỉ văn bản dán như reviewer nói), idea.md L152 đã yêu cầu VLM trả "JSON trạng thái màn hình" có cấu trúc, và prompt.md L192/L462 đã bắt security-redteam thử injection "qua ảnh, qua kết quả tool giả". Khoảng trống còn lại: (1) "tóm tắt có cấu trúc" của quaranti… → ADR-004 phải ghi nguyên tắc "planner không bao giờ nhận chuỗi tự do có nguồn gốc không tin cậy" và hiện thực bằng schema: (a) quarantine trả về Pydantic model kín (enum intent, mã red_flag, số tiền, tên cơ quan từ danh mục) — trường text tự do bị cấm ở schema, có test; (b) chunk RAG chỉ vào planner sau verify_citation, được bọc nhãn nguồn và lọc câu mệnh lệnh (regex + judge nhỏ), tối đa N câu; (c) output VLM ép về `screen_id` thuộc catalog kịch bản + danh sách `element_id`, không trường text; (d) `user_id` lấy từ J…
- **SEC-04** [major] (security-legal-ethics) privacy — schema DB, log và thời hạn lưu — Nguyên tắc bất biến (2) "không lưu dữ liệu định danh" (plan L31, L468; idea L238) chỉ được định nghĩa hẹp là CCCD/số tài khoản/OTP/mật khẩu, trong khi schema mục 5 vẫn cho phép lưu nội dung tự do người dùng gõ/hỏi qua `events.payload_json` (type input/ask, L189) không có allowlist trường, `screen_jobs.boxes_json` và `audit.target` không định nghĩa nội dung, và `users.display_name` + `classes.ward_code` + lớp ~30 ngườ… → (1) Định nghĩa schema JSON cho từng `type` của events với allowlist trường: input ⇒ {field_id, valid:bool, len}; ask ⇒ {intent_class, hmac_sha256(question, server_salt)} hoặc không lưu; boxes_json ⇒ [{cls, x, y, w, h}] cấm text; audit.target ⇒ uuid. (2) display_name = biệt danh/mã do TNV cấp, không họ tên; họ tên chỉ trên phiếu giấy do trưởng nhóm giữ, hủy sau 90 ngày. (3) Bảng retention chuẩn trong ADR-005: events/sessions/qa_logs/drills 90 ngày, log app 30 ngày, backup 14 bản × 6 h ⇒ quy trình xóa theo yêu cầu ph…
- **SEC-05** [major] (security-legal-ethics) privacy/security — che PII ảnh màn hình app thật — Mô-đun kèm cặp yêu cầu người cao tuổi chụp màn hình VNeID/ngân hàng thật (CCCD, số tài khoản, số dư, có thể cả SMS OTP) nhưng che PII bằng "regex + YOLO" trong khi YOLOX chỉ được huấn luyện trên ảnh sandbox tự render; không có chỉ số recall che PII nào trong eval; VLM có thể chép số vào `screen_state`/`boxes_json` và được lưu; 200 "ảnh app thật" trong eval/sets và checklist "chụp 10 màn hình app thật" đưa ảnh cá nhân… → (1) Thêm bộ eval `pii-redaction` vào `make eval`: ≥ 500 ảnh mock VNeID/eTax/ngân hàng có CCCD/STK/OTP giả ở nhiều font/độ sáng/góc chụp; ngưỡng chặn phát hành recall ≥ 0,99 (một lần sót = lỗi). (2) Dùng OCR (PaddleOCR/EasyOCR, giấy phép Apache) + regex làm lớp che chính, YOLOX làm lớp phụ; che TRƯỚC khi ảnh vào MinIO. (3) Prompt VLM cấm chép chuỗi số ≥ 4 ký tự; post-filter regex trên output VLM và trên `say` của planner ("không bao giờ đọc số từ màn hình"); red-team bắt buộc có ca "ảnh chứa OTP/số dư" = 0 rò rỉ. (4…
- **SEC-06** [major] (security-legal-ethics) ethics/security — mô-đun vắc-xin lừa đảo — Schema drills (plan.md L622) không có trường nào chứa nội dung tin nhắn/lời TTS mà idea.md L85, L94 yêu cầu, đồng thời rule "không có trường chứa lời thoại hoàn chỉnh" và tiêu chí E09 (L215) không đo được — Claude Code sẽ phải tự bịa trường nội dung ở E09. Phần "khóa sau đăng nhập lớp" (idea.md L257) đúng là chỉ QR tĩnh + tên gọi (plan.md L165, L186, không có hết hạn/thu hồi qr_token), nhưng rủi ro bị giảm nhẹ vì rat… → Thay rule mơ hồ bằng ràng buộc đo được trong schema: thêm `utterance` (đúng 1 lượt, ≤ 40 từ, tiếng Việt, bắt buộc chứa nhãn "[Mô phỏng]" khi TTS), cấm URL/số điện thoại/số tài khoản/mã QR/tên ngân hàng–cơ quan thật (regex), không chuỗi nhiều lượt "leo thang", `impersonates` chỉ là danh mục; validator chạy trong `make data` + duyệt tay 100 %. API `/drills` chọn biến thể phía server, không có endpoint liệt kê toàn bộ; rate limit theo user; `qr_token` hết hạn sau buổi học và TNV có thể thu hồi. Thêm vào red-team: "viế…
- **LIC-07** [minor] (security-legal-ethics) licensing — YOLO/YOLOX và PhoWhisper — idea.md còn dùng "YOLO" chung chung (L15, L92, L109, L136, L153, L166, L177, L219, L270, L302, L390, L411, L415, L452 "Theo giấy phép của bản dùng") trong khi plan.md (L158, L482, L488) và prompt.md (L39, L144 ADR-002, L181, L205) đã chốt nhất quán YOLOX-Tiny Apache-2.0 và giới hạn thư viện ngoài danh sách phải hỏi trước; idea.md L451 kê PhoWhisper là Apache-2.0 trong khi giấy phép thật là BSD-3-Clause, và không có d… → Sửa toàn bộ "YOLO" trong idea.md (L18, L136, L153, L452, L462 sơ đồ) thành "YOLOX-Tiny (Apache-2.0)". Thêm vào CLAUDE.md danh sách gói cấm (`ultralytics`, `yolov5`, mọi gói AGPL/GPL/NC trong runtime) và cổng `make check` chạy `pip-licenses` + `license-checker` (npm) fail khi gặp AGPL/GPL/CC-NC trong dependency runtime. Sửa registry models.yaml `PhoWhisper: license: BSD-3-Clause` và thêm file NOTICE với điều khoản BSD; vì bản kê khai sinh từ registry nên chỉ cần sửa một chỗ.
- **LIC-08** [minor] (security-legal-ethics) licensing — VIVOS (CC BY-NC-SA) và TTS — Tập đánh giá "10 giờ audio" trong `eval/sets/` (plan.md L243) có phần phái sinh từ VIVOS (CC BY-NC-SA 4.0) nhưng ba tài liệu không nói audio này chỉ lưu manifest/tải lại hay được commit vào repo public Apache-2.0 (plan.md L343), và idea.md L179 ghi mơ hồ "giấy phép mở"; TTS runtime (idea.md L150) chưa chốt ở ADR-002 trong khi checkpoint F5-TTS thường CC-BY-NC-4.0 nên cần kê khai giấy phép NC và ghi hạn chế triển khai… → Registry thêm trường `redistribute: true/false` và `derived_from`; `eval/sets/audio` chỉ chứa manifest + script tải lại từ nguồn gốc và tái tạo nhiễu theo seed, không commit audio VIVOS/derived; báo cáo WER tách theo nguồn (Common Voice CC0 có thể công bố). Chọn TTS có giấy phép rõ (Piper MIT với giọng Việt có giấy phép tương thích, hoặc model Apache/MIT) và chốt trong ADR-002 trước E04; nếu vẫn dùng F5-TTS NC, kê khai NC trong bản kê khai và ghi rõ hạn chế triển khai ở mục 11/12 hồ sơ.
- **LIC-09** [major] (security-legal-ethics) licensing/ethics — dùng API LLM thương mại sinh dữ liệu huấn luyện — Ba tài liệu chốt dùng "API LLM thương mại" (không nêu provider) để sinh 8.000 SFT + 2.000 DPO fine-tune Qwen (idea.md L164, L457; plan.md L135, L615) và đồng thời tuyên bố phát hành dữ liệu tổng hợp đó theo CC BY 4.0 (idea.md L205, L455), nhưng không có dòng nào về điều khoản sử dụng của provider; nếu provider là OpenAI/Anthropic/Google thì có nguy cơ vi phạm ToS (cấm dùng output huấn luyện mô hình cạnh tranh) và đội… → Chốt trong ADR-002: sinh dữ liệu và làm judge bằng model mở có giấy phép cho phép dùng output (Qwen3.5 cỡ lớn/Qwen3-235B, DeepSeek… qua nhà cung cấp hosting mà ToS không cấm, hoặc tự host tạm trên GPU thuê) hoặc self-instruct bằng chính Qwen3.5-9B base — vẫn kê khai đầy đủ. datasets.yaml thêm `generator_model`, `generator_license`, `tos_url`, `tos_checked_on`; bản kê khai ghi rõ tên provider/model cho cả sinh dữ liệu lẫn judge. Nếu bắt buộc dùng API thương mại, đọc ToS ngày hiện tại, lưu bản chụp vào Drive minh chứ…
- **LIC-10** [minor] (security-legal-ethics) legal — crawl cổng nhà nước và trang ngân hàng — Ba file idea/plan/prompt không nói rõ `data/raw/guides/` và `data/clean/guides/` có được loại khỏi repo public hay không, trong khi idea.md L205 tuyên bố "không phân phối lại"; tuy nhiên docs/decisions/E01-brief.md L47 đã ghi `raw/ clean/ ui/ sft/ dpo/ (gitignore)`, nên rủi ro phân phối lại thực tế đã được xử lý ở tầng quyết định. Phần còn thiếu chỉ là: plan.md mục 7/registry chưa có trường `source_type` (QPPL / cơ q… → gitignore `data/raw/`, `data/clean/`, snapshot Qdrant; repo chỉ chứa `data/sources.yaml` + `manifest.jsonl` (url, sha256, ngày, loại nguồn) + script tái tạo `make data`. sources.yaml thêm trường `source_type` (văn bản QPPL / hướng dẫn cơ quan nhà nước / ngân hàng) và `tos_checked` (ngày, ghi chú robots + điều khoản sử dụng); trang ngân hàng chỉ dùng khi ToS không cấm. Giao diện chỉ hiển thị đoạn trích ≤ 2 câu kèm link nguồn (trích dẫn hợp lý có ghi nguồn). Bản kê khai mục Dataset ghi từng nhóm nguồn với cơ sở sử dụ…
- **LEG-11** [major] (security-legal-ethics) legal — trích dẫn văn bản pháp luật chưa xác minh — Ba văn bản chưa kiểm chứng (Thông tư 05/2026/TT-BKHCN, Luật BVDLCN 91/2025/QH15, Nghị định 356/2025/NĐ-CP) được nêu như đã có với ngày hiệu lực cụ thể; idea.md có chú thích "tra cứu trước khi trích" nhưng plan.md/prompt.md không có bước nào thực hiện, nên docs-writer sẽ chép nguyên vào PDF; "tự phân loại rủi ro thấp" chưa dẫn tiêu chí cụ thể trong NĐ 142/2026. → Thêm `docs/legal/refs.yaml` (số hiệu, tên, ngày ban hành, hiệu lực, điều khoản áp dụng, url vbpl.vn/chinhphu.vn, `verified_on`, `verified_by` = tên thành viên) và cổng trong `make dossier`: mọi số hiệu văn bản xuất hiện trong PDF phải khớp một mục có `verified_on` không rỗng, nếu không thì fail. Giao việc tra cứu cho một thành viên người (không giao Claude) trước D20, đặc biệt đọc điều khoản phân loại rủi ro của NĐ 142/2026 và ghi căn cứ vào mục 11; nếu TT 05/2026 không tồn tại thì thay bằng văn bản khung đạo đức A…
- **ETH-12** [minor] (security-legal-ethics) ethics/privacy — thiết kế pilot và nơi lưu dữ liệu — Hai điểm thật sự chưa khớp: (1) plan.md L320/idea.md L125 dùng Zalo để ôn tập và đo "tỷ lệ quay lại" nhưng bảng users (plan.md L185) không có cột liên hệ nào và không nói cách gắn lượt quay lại với mã số; (2) idea.md L243 và plan.md L327 cam kết "máy chủ trong nước" trong khi plan.md L131/idea.md L266 chọn "RTX 4090 thuê hoặc A10G cloud" (A10G chỉ có trên AWS, chưa có vùng Việt Nam) và Cloudflare proxy (plan.md L132,… → (1) Bỏ Zalo khỏi pilot (thay bằng thẻ giấy + QR ôn tập không cần đăng nhập, đo "quay lại" bằng mã lớp) hoặc thêm cột `contact_zalo` nullable + `contact_consent_at`, xóa cùng dữ liệu, ghi rõ trong phiếu và ADR-005. (2) `/speech/asr` mặc định xử lý trong bộ nhớ, không ghi file, có test; phiếu nêu "giọng nói được chuyển thành chữ ngay và không lưu"; audio cho ASR LoRA chỉ khi có ô đồng ý riêng. (3) Chọn GPU cloud trong nước (FPT/Viettel/VNG/VNPT) cho prod/pilot; Cloudflare để DNS-only; nếu không được, ghi trung thực ở…
- **CC-01** [blocker] (claude-code-config) hooks / Stop (type agent) — Hook `Stop` kiểu `agent` với prompt "make check đã xanh chưa… Nếu chưa, trả lời 'chưa xong'" sẽ chạy ở MỌI lần Claude kết thúc lượt (Stop không phải sự kiện 'kết thúc epic'), chặn Claude dừng khi make check chưa xanh, không xét `stop_hook_active` nên có thể lặp vô hạn, và phá ngay luồng E01: Master Prompt yêu cầu chạy trong plan mode và 'trình kế hoạch trước, chỉ thực thi sau khi tôi duyệt' — trong plan mode Claude k… → Bỏ hoàn toàn hook `agent` khỏi `Stop`. Chuyển 'cổng epic' vào bước 5 của skill `/epic` (prompt.md:496) dưới dạng checklist bắt buộc trước khi mở PR, và vào CI (GitHub Actions chạy make check trên PR — đã có ở prompt.md:145). Nếu vẫn muốn hook cứng: dùng `type: command` Python `.claude/hooks/epic-gate.py` chỉ chặn khi (a) tồn tại marker `docs/status/EPIC_IN_PROGRESS`, (b) `stop_hook_active` == false trong JSON stdin, (c) `.cache/make-check.ok` cũ hơn commit mới nhất; mọi trường hợp khác `sys.exit(0)`. Cấu hình: `"St…
- **CC-02** [major] (claude-code-config) hooks / promptlog.sh (Prompt Log bắt buộc của BTC) — promptlog.sh ở prompt.md §9.3 (script mà "Trình tự ngày 0" bảo chép nguyên vào .claude/) là thiết kế sai cho Prompt Log bắt buộc: phụ thuộc jq (máy dev không có → thoát 0 im lặng, không ghi gì), chép trọn transcript ở mỗi Stop (phình O(n²), INDEX hàng chục dòng/phiên), tên file theo phút gây ghi đè và hash INDEX lệch (tự tạo dấu hiệu "bị sửa sau khi ghi" mâu thuẫn plan.md:345), không xử lý --close, transcript JSONL k… → Viết lại thành `.claude/hooks/promptlog.py` (stdlib: json/hashlib/shutil/subprocess/datetime): đọc `json.load(sys.stdin)`; ở `Stop`: `shutil.copyfile(transcript_path, f"docs/prompt-log/sessions/{session_id}.jsonl")` (ghi đè idempotent, KHÔNG ghi INDEX); ở `SessionEnd`: chép lần cuối, tính sha256, ghi thêm 1 dòng `| <ISO time> | sessions/<session_id>.jsonl | <git rev-parse --short HEAD hoặc 'nogit'> | <sha256> |` vào INDEX.md, đồng thời snapshot 'system prompt' = zip/hash của CLAUDE.md + .claude/agents + .claude/ski…
- **CC-03** [major] (claude-code-config) subagents / model alias — `model: fable` (frontmatter của 10 subagent) và `CLAUDE_CODE_SUBAGENT_MODEL: "fable"` không phải alias hợp lệ của Claude Code 2.1.119: binary chỉ có alias `sonnet`/`opus`/`haiku`/`inherit`, không có chuỗi `fable` và thậm chí không có `claude-fable-5-1`. Giá trị lạ sẽ được truyền thẳng làm model ID → API trả lỗi model không tồn tại → mọi lần gọi subagent thất bại ngay E01. → Dùng `model: inherit` trong cả 10 file `.claude/agents/*.md` (kế thừa `"model": "claude-fable-5-1"` của settings.json:343, vốn hoạt động vì là full ID pass-through). Xóa `CLAUDE_CODE_SUBAGENT_MODEL` khỏi `env` (hoặc đặt đúng full ID `claude-fable-5-1` nếu muốn ép). Sửa bảng prompt.md:33-44 cột 'Model / effort' thành `inherit / <effort>`.
- **CC-04** [minor] (claude-code-config) settings.json / env — prompt.md:346-347 đặt hai biến `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS` / `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` không tồn tại trong CLI 2.1.119 (no-op, không ép gì), và prompt.md:18 nêu "mặc định 20 / 3" không kiểm chứng được; giới hạn 6 đồng thời / 2 tầng thực tế chỉ tồn tại dưới dạng luật văn bản trong CLAUDE.md (prompt.md:72, :159). Cần xóa 2 khóa env và sửa dòng 18; docs/decisions/E01-brief.md D3 đã ghi nhận quyết… → Xóa 2 khóa khỏi `env`. Giữ giới hạn dưới dạng luật trong CLAUDE.md mục Delegation (đã có ở prompt.md:72) và trong skill `/epic`. Nếu cần ép cứng: hook `PreToolUse` matcher `Task|Agent` (Python) đếm số spawn đang mở qua file `.cache/subagents.json` (tăng ở PreToolUse, giảm ở `SubagentStop`), `sys.exit(2)` khi ≥ 6.
- **CC-05** [minor] (claude-code-config) effort / lệnh phiên bản-gated — `/effort ultracode` (prompt.md:15, :314) và `/subtask` (prompt.md:311) không tồn tại trong Claude Code 2.1.119 đang cài (grep claude.exe: ultracode=0, /subtask=0, trong khi effortLevel=13, CLAUDE_CODE_EFFORT_LEVEL=7); ultracode chỉ có ở CLI ≥ 2.1.203. Mẫu settings.json (prompt.md 9.1) không đặt `effortLevel` mặc định dù §1 khuyến nghị xhigh làm nền. Tuy nhiên skill `/epic` (9.8) đã mã hóa sẵn toàn bộ quy trình plan →… → Thay mọi chỗ 'ultracode' bằng: `/effort xhigh` + plan mode + fan-out subagent song song được viết tường minh trong skill `/epic` bước 2 (đã có); thêm ghi chú 'chạy `claude update`; chỉ dùng `/effort ultracode` khi `claude --version` ≥ 2.1.203, ghi phiên bản vào DAILY.md'. Thay `/subtask` bằng skill có `context: fork` hoặc gọi subagent. Thêm `"effortLevel": "xhigh"` vào `.claude/settings.json` (không phải `effort`).
- **CC-06** [major] (claude-code-config) hooks / Windows + bộ kit ngày 0 thiếu — settings.json (prompt.md:363-379) có 10 mục hook lệnh trỏ tới 9 script `.sh` khác nhau, cộng `budget.sh` được guard.sh gọi (:395) = 10 file cần có, nhưng Phụ lục chỉ cung cấp 2 (guard.sh, promptlog.sh); `daily.sh` có ở bảng :117 nhưng không có trong settings; tất cả dùng bash + `jq` và đường dẫn tương đối không interpreter, trong khi plan.md:129 giả định máy dev Ubuntu còn máy thật là Windows 11 (không có jq/make) → … → Viết tất cả hook bằng Python stdlib và gọi `"command": "python .claude/hooks/<tên>.py"` (cwd = thư mục dự án; dùng `os.environ.get('CLAUDE_PROJECT_DIR', '.')` để an toàn — biến này có trong binary). Bộ kit ngày 0 phải chứa đủ 10 file: session-start.py, prompt-guard.py, guard.py, budget.py, protect-paths.py, format.py, notify.py, subagent-log.py, checkpoint.py, promptlog.py (stub `sys.exit(0)` được phép), cộng `docs/status/DAILY.md`, `training/budget.json`. Thêm 1 câu vào Master Prompt: 'Trong E01 được phép tạo/hoàn…
- **CC-07** [major] (claude-code-config) hooks / guard.sh + budget — Regex của guard.sh sai theo cả hai chiều: `python .*train` dương tính giả với lệnh test thường gặp (đã kiểm chứng: `uv run python -m pytest tests/agent/test_constraints.py` và `python scripts/check_pretrained_download.py` đều khớp) → gọi budget.sh (chưa tồn tại) → nhánh `||` → exit 2 → chặn; còn regex `.env` bỏ lọt `cat ./.env`, `cat services/api/.env`, `python -c "open('.env')"` (đã kiểm chứng) trong khi deny `Read(… → guard.py: chặn train/data theo tiền tố chính xác `re.match(r'^(make (train|data|finetune)\b|uv run (training/|data/pipeline/))', cmd)` rồi gọi budget.py; budget.py đọc `training/budget.json` `{"gpu_hours_limit":20,"gpu_hours_used":0,"api_vnd_limit":500000,"api_vnd_used":0}` (script train/synth tự cộng dồn sau mỗi run) và exit 2 khi dự kiến vượt; chặn `.env` bằng `re.search(r'(^|[\s/"\'=])\.env(\.[\w-]+)?\b', cmd)` trên toàn lệnh; giữ danh sách lệnh phá hủy nhưng dùng `\brm\s+-[a-z]*r[a-z]*f` để bắt cả `rm -fr`. Tạo…
- **CC-08** [major] (claude-code-config) hooks / protect-paths.sh — protect-paths không thể thực hiện hai điều tài liệu hứa: (a) 'chặn sửa config/eval.yaml ngoài PR có nhãn eval-threshold' — hook chạy lúc Edit, khi PR có thể chưa tồn tại, `gh` không có trên máy, và ngay E01 phải TẠO config/eval.yaml → bị chặn; (b) 'file migration đã merge' cần lịch sử git nhưng repo chưa có commit → kiểm tra thất bại/không xác định. Thêm nữa matcher `Edit|Write` bỏ sót `MultiEdit` (vẫn có trong binar… → Matcher: `"Edit|MultiEdit|Write|NotebookEdit"` cho cả PreToolUse (protect-paths) và PostToolUse (format); deny thêm `"MultiEdit(docs/prompt-log/**)"`. protect-paths.py: migration = chặn khi `git cat-file -e main:<path>` trả 0 (file đã có trên main), fail-open khi git không có HEAD; cổng ngưỡng eval chuyển sang CI: job GitHub Actions kiểm tra nếu diff chạm `config/eval.yaml` thì PR phải có label `eval-threshold`; cục bộ cho phép khi nhánh khớp `epic/*eval-threshold*` hoặc `CTCV_ALLOW_EVAL_THRESHOLD=1` trong settings…
- **CC-09** [minor] (claude-code-config) subagents / tools vs nhiệm vụ — Bảng agent mâu thuẫn một phần với nhiệm vụ: (a) `architect` có `permissionMode: plan` (chế độ chỉ đọc/khám phá theo docs chính thức) nhưng prompt.md:423 và Master Prompt A.(5) (prompt.md:144) yêu cầu nó "soạn ADR vào docs/decisions/" / "viết ADR-001..005" → sẽ bị chặn ghi, cần chuyển việc ghi file cho orchestrator/docs-writer (vốn đã có vai trò ADR ở dòng 44); (b) `reviewer` và `security-redteam` được gắn nhãn "Chỉ đ… → architect: giữ read-only, đầu ra trả nội dung ADR trong tóm tắt và orchestrator/docs-writer ghi file; hoặc thêm `Write, Edit` và ép phạm vi bằng protect-paths.py (architect chỉ được ghi `docs/decisions/**`, `.claude/agent-memory/architect/**` — đọc `agent`/`cwd` từ JSON stdin không có tên agent, nên dùng quy ước: hook cho phép ghi memory dir cho mọi agent, còn phạm vi ghi khác kiểm theo đường dẫn). security-redteam: `tools: Read, Grep, Glob, Bash, Write, Edit` + hook chỉ cho ghi `eval/redteam/**`, `docs/security/**…
- **CC-10** [major] (claude-code-config) subagents / qa-tester background — `qa-tester` được khai `background: true` (prompt.md:40, :441) nhưng lại là cổng đồng bộ bắt buộc ("test xanh" trước `reviewer` — prompt.md:70, :282-284, :494); với CLI 2.1.119 (< 2.1.186) subagent nền bị auto-deny mọi lệnh ngoài allow-list và chỉ có bộ tool rút gọn, kết quả chỉ về ở lượt sau nên orchestrator có thể gọi `reviewer`/mở PR trước khi test xong. Lưu ý: `make *`, `pytest *`, `mcp__playwright` đã nằm trong a… → Bỏ `background: true` khỏi qa-tester.md. Nếu muốn chạy eval đêm không chặn, tạo agent riêng `eval-runner` (`background: true`, tools Read/Bash, chỉ gọi từ `/daily` hoặc CI). Thêm vào allow: `"Bash(npx playwright *)"`, `"Bash(python *)"`, `"Bash(uv run *)"` (đã có qua `uv *`), `"Bash(gh pr *)"`.
- **CC-11** [major] (claude-code-config) subagents / isolation: worktree — `isolation: worktree` cho backend-dev/frontend-dev và quy tắc "mỗi việc trên worktree riêng" (prompt.md:19, 36-37, 158, 493; E01-brief §16 chia 11 gói F..K chạy song song) không dùng được ở E01 vì repo chưa có commit nào: với git 2.53 trên máy dev, `git worktree add` không fatal mà tự suy ra `--orphan` và tạo worktree RỖNG (chỉ có file `.git`, không có docs/, CLAUDE.md, .claude/ vì các file này còn untracked) → subag… → Ngày 0: `git add docs .claude .mcp.json .gitignore && git commit -m "chore(E00): bootstrap docs and Claude Code config"` TRƯỚC phiên claude đầu tiên (đồng thời là mốc đầu của commit history cho BTC). E01: gọi backend-dev/frontend-dev không dùng worktree (thêm câu 'E01 không dùng isolation worktree' vào Master Prompt B). Từ E02: Makefile đặt `COMPOSE_PROJECT_NAME=ctcv-$(shell git branch --show-current | tr '/' '-')` và biến `PORT_OFFSET` theo nhánh; `make dev` cache `uv`/`pnpm` store dùng chung (`UV_CACHE_DIR`, `pnp…
- **CC-12** [minor] (claude-code-config) MCP (.mcp.json, permissions) — Cấu hình MCP có vài điểm cần sửa nhưng đều nhỏ: (1) gốc repo có file `mcp.json` rỗng 0 byte (sai tên, Claude Code không đọc) trong khi docs đã dùng đúng `.mcp.json` ở mọi chỗ (prompt.md:25,79,100,123,500) → chỉ cần xóa file thừa; (2) `@modelcontextprotocol/server-github` (prompt.md:506) đã bị npm đánh dấu "Package no longer supported" (latest 2025.4.8) — docs/decisions/E01-brief.md:175 đã ghi nhận "server tham chiếu … → Xóa `mcp.json` rỗng, tạo `.mcp.json`: `{"mcpServers":{"playwright":{"type":"stdio","command":"cmd","args":["/c","npx","-y","@playwright/mcp@latest","--headless"]}}}` (bỏ github MCP; dùng `gh pr create/view --comments` qua Bash với allow `"Bash(gh *)"`; sửa skill /review :240 thành `gh pr view $ARGUMENTS --comments`). Trong settings.json: `"enableAllProjectMcpServers": false, "enabledMcpjsonServers": ["playwright"]` thay cho deny `mcp__*`; giữ allow `"mcp__playwright"`. Ghi chú: trên máy Linux/CI đổi lại `"command":…
- **CC-13** [minor] (claude-code-config) plugin ctcv-kit / bố cục — prompt.md:101, :125, :522 đặt `agents/`, `skills/`, `hooks/hooks.json`, `.mcp.json` ở gốc repo cạnh `.claude-plugin/` — trùng lặp thủ công với `.claude/agents|skills|hooks` và sẽ lệch nhau theo thời gian; nhưng `docs/decisions/E01-brief.md` (§1 dòng 35, §10 target `plugin`, §11 dòng 176, §16 gói A) đã chốt đúng hướng: `plugins/ctcv-kit/` sinh bởi `scripts/sync_plugin.py` (bỏ `hooks`/`mcpServers`/`permissionMode`, sin… → Đặt plugin ở `tools/ctcv-kit/` với `.claude-plugin/plugin.json`, `agents/`, `skills/`, `hooks/hooks.json`, `.mcp.json`; `.claude/` vẫn là nguồn sự thật cho công việc hằng ngày; thêm target `make kit-build` (script Python chép `.claude/agents skills hooks` → `tools/ctcv-kit/`, loại bỏ 3 trường bị bỏ qua, sinh hooks.json từ settings.json) và `make kit-validate` = `claude plugin validate tools/ctcv-kit`. Ngày hackathon: `claude --plugin-dir tools/ctcv-kit` trong repo mới (hoặc marketplace cục bộ). Cập nhật prompt.md:1…
- **CC-14** [major] (claude-code-config) nhất quán prompt.md ↔ plan.md (Claude sẽ vấp khi bootstrap) — Hai tài liệu mô tả hai luồng Day-1 khác nhau và Claude Code sẽ đọc cả hai: plan.md P0 bảo 'Sinh CLAUDE.md theo Phụ lục A', tự tạo 'hook promptlog' trong E01 và các ngày sau gõ P1–P5 ('Thực hiện epics/E0X.md'); prompt.md Master Prompt giả định `.claude/` + hook đã có từ ngày 0, gọi architect viết 5 ADR, các ngày sau chỉ dùng `/daily`, `/epic`. Tên file prompt log cũng khác (plan.md `.md` + `E0X`; prompt.md `.jsonl` + … → Chọn prompt.md là nguồn sự thật cho prompt/cấu hình: thay plan.md §12 bằng 'Xem docs/prompt.md §4–§6; P0 = Master Prompt, P1 = /epic, P2 = /daily, P3 = /review, P4 = D1, P5 = D6, P6 = /hackathon'; sửa plan.md:87 và :121 cho khớp (`/epic E0X`; `docs/prompt-log/sessions/<session_id>.jsonl`). Thêm vào Phụ lục A mục '## Delegation (subagent, skill, hook)' ≈ 12 dòng chép 6 quy tắc ở prompt.md:68-73 + câu 'hooks/settings chỉ đổi qua ADR' (tổng ≈ 70 dòng, vẫn < 150). Sửa plan.md:486 thành `planner_small: Qwen3.5-2B` (hoặc…
- **S1** [blocker] (timeline-risk) Lịch D1–D21 vs hạn thật — Toàn bộ lịch D1–D21 của plan.md đặt epic 'Hồ sơ' ở D20–D21 (= 7–9/10/2026 nếu D1 = 18 hoặc 19/9), tức 8–9 ngày SAU hạn nộp thực tế 30/9 (tuyến trường); tuyến đội tự do 20/9 (Chủ nhật, 2 ngày nữa, repo chưa có commit) đã chết nhưng idea.md và plan.md vẫn coi là lựa chọn mở. Hai mốc rà phạm vi D10/D17 rơi vào 27–28/9 và 4–5/10 — mốc thứ nhất chỉ 2 ngày trước hạn, mốc thứ hai sau hạn — nên cơ chế tự cắt phạm vi vô dụng … → Trước khi chạy bất kỳ prompt E01 nào: sửa plan.md §2 và §6 thành 3 pha theo bảng mốc trong phần tóm tắt (Pha A hồ sơ sơ bộ 18/9–29/9; Pha B bộ kit hackathon 1/10–9/10; Pha C cổng quyết định; Pha D sản phẩm v1.0 cho chung kết 13/10–17/11). Xóa tuyến 20/9 khỏi idea.md §11–12 và plan.md §4; đổi 'v1.0 D21' thành 'v0.5-dossier 29/9'. Đặt mốc rà phạm vi ở 22/9 (sau v0.1) và 25/9 (freeze) thay cho D10/D17. Nếu D1 = 19/9, bỏ trước kịch bản thứ 5–6 và ablation thành phần, giữ nguyên các ngày 25–29/9.
- **S2** [blocker] (timeline-risk) Tuyến trường: hạn nội bộ, đội hình — Tuyến trường đòi hỏi vòng xét chọn nội bộ của trường (thi/xét hồ sơ/phỏng vấn), danh sách gửi trước 30/9, tối đa 5 đội Bảng C/trường, 3 thành viên cùng trường và đội hình đóng băng sau đăng ký. Cả ba tài liệu chỉ ghi việc này như một ô checkbox ngày 0 (plan.md dòng 140 'Hạn 20/9 hoặc 30/9'; idea.md dòng 316, 349 'liên hệ Đoàn trường ngay') mà không có mốc, người chịu trách nhiệm hay bàn giao tối thiểu cho vòng nội bộ… → Ngay 18/9: gọi Đoàn trường/Phòng CTSV TDTU để lấy (a) hạn nội bộ, (b) định dạng xét (hồ sơ? phỏng vấn? demo?), (c) còn suất không; chốt danh sách 3 thành viên cùng trường và nộp đơn xin giấy xác nhận SV cùng ngày (giao thành viên 2). Thêm mốc 'Vòng nội bộ trường' vào plan §2 với bàn giao tối thiểu: pitch 1 trang + video demo 2 phút + URL chạy được, sẵn sàng trước hạn nội bộ 1 ngày. Ghi rõ trong plan §4: 'tuyến trường; 20/9 đã loại'.
- **S3** [blocker] (timeline-risk) Pilot 30 người trước hạn hồ sơ — Với D1 = 18/9, lịch 21 ngày của plan.md đặt Pilot ở D17–D19 (~4–6/10) và Hồ sơ ở D20–D21 (~7–8/10), tức cả pilot lẫn hồ sơ đều rơi sau hạn 30/9 (= D13, trước cả khi v0.5 xong ở D14); trong khi plan.md dòng 222 buộc 'E12 → Pilot → Hồ sơ', dòng 311 gọi pilot là 'bằng chứng quyết định của hồ sơ' và DoD #7 (dòng 28) yêu cầu ≥ 30 người. Các phương án dự phòng (idea.md 388, 396; plan.md 330, 376) chỉ giảm quy mô xuống 10–1… → Bằng chứng thay thế cho hồ sơ 30/9 (đủ cho mục 7–9 MẪU 3): (1) eval tự động quy mô nhỏ nhưng thật: 100 Q&A citation-support, 100 hội thoại (≤2 câu, xác nhận ý định), red-team 50 = 0, load 10 người, p95; (2) ablation THÀNH PHẦN cùng model base (RAG bật/tắt, guardrail bật/tắt, xác nhận ý định bật/tắt) — làm được trong 1 ngày, không cần fine-tune, trả lời trực tiếp mục 9; (3) thử nghiệm sơ bộ n = 5–10 người 50+ (người thân/hàng xóm) ngày 26–27/9 có phiếu đồng thuận: SUS, tỷ lệ hoàn thành, thời gian — ghi rõ 'sơ bộ, kh…
- **S4** [major] (timeline-risk) Hackathon 2 ngày (60%) chưa được ưu tiên đúng — 60% điểm khu vực đến từ hackathon 2 ngày trên dataset BTC, nhưng trong 9 ngày giữa hạn hồ sơ và ngày thi (1–9/10) lịch cũ dùng cho E10 (kịch bản 7–12), E12 (APK offline) và pilot; không có ngày nào cho việc tách bộ kit và diễn tập (plan §2 chỉ ghi 'Diễn tập 8 giờ xong' như điều kiện phát hành v1.x, không có trong §6). Bộ kit ở idea §13 và skill /hackathon giả định bài toán dạng RAG/agent (giờ 2–6 'dựng ingestion + RA… → Pha B 1–9/10: (1) 1–3/10 tách plugin ctcv-kit + repo mẫu `hackathon-template/` gồm: ingestion CSV/Excel/PDF/HTML; EDA tự động + baseline ML dạng bảng (pandas, scikit-learn/LightGBM, cross-validation, bảng metric) + biểu đồ; RAG trích dẫn; eval harness chung; dashboard nhanh; deploy CPU-only (model 2B/4B GGUF hoặc API nếu BTC cho phép); sinh slide/hồ sơ; (2) 4/10 diễn tập #1 8 giờ với đề dạng bảng, 7/10 diễn tập #2 6 giờ với đề dạng văn bản; (3) cả 3 thành viên cài Claude Code + kit trên laptop riêng và mỗi người ch…
- **S5** [major] (timeline-risk) Tiêu chí nghiệm thu 1 ngày/epic không thực tế — Lịch epic không khớp hạn nộp và một số tiêu chí nghiệm thu thiếu nguồn/thiếu khả thi: với D1 = 18/9, E06–E08 rơi vào 25–28/9 và Hồ sơ (D20–D21) rơi vào 7–8/10, tức sau cả hạn 30/9 tuyến trường; plan §6 chỉ có phương án lùi khi E07 trễ (E08–E10 chạy base, dòng 222) và quy tắc cắt ở D10 (dòng 376) chứ không có ngưỡng nghiệm thu riêng cho bản nộp sớm. Về kỹ thuật: LoRA bf16 Qwen3.5-9B seq 4k trên 24 GB (plan dòng 250) r… → Tách ngưỡng theo phiên bản: v0.5-dossier (29/9) dùng tập nhỏ đo thật (≥300 trang, 100 Q&A, 100 hội thoại, 1 giờ audio theo giọng, red-team 50, load 10 người) và báo cáo số đo được thay vì ngưỡng; giữ ngưỡng đầy đủ của §7 cho v1.0 chung kết. Chuyển E06/E07/E08 sang Pha D (13–26/10) với QLoRA 4-bit cho 9B hoặc LoRA đầy đủ trên Qwen3.5-4B; chỉ làm fine-tune trước 30/9 nếu E05 xanh trước tối 23/9 VÀ GPU đã sẵn — nếu không, hồ sơ ghi 'base + prompt có cấu trúc, pipeline dữ liệu và ablation thành phần đã sẵn' như idea.md…
- **S6** [major] (timeline-risk) Quy tắc cắt phạm vi đã kích hoạt từ D1 nhưng không đủ — Quy tắc cắt phạm vi chỉ kích hoạt khi 'trễ > 1 ngày' ở D10/D17 và chỉ bỏ DPO, kịch bản 10–12, APK, LoRA giọng — giải phóng ~3 ngày — trong khi ngay D1 khoảng cách giữa lịch và hạn 30/9 là 8–9 ngày. 'Phạm vi tối thiểu chắc chắn nộp được' của idea §14 vẫn gồm pilot 10 người + đủ 6 thành phần + vắc-xin 10 kịch bản + dashboard, tức chính bản tối thiểu cũng không lắp vừa 12 ngày nếu giữ nguyên thứ tự epic. → Thay quy tắc 'trễ > 1 ngày' bằng danh sách phạm vi theo pha, ghi vào plan §2: v0.5-dossier (29/9) = E01, E02 (4→6 kịch bản), E03-lite, E04, E05 (model base + CPU fallback), E09-lite (10 kịch bản), E10-lite (dashboard lớp + 1 báo cáo), E12-lite (URL công khai + status page, không APK), eval nhỏ + ablation thành phần, thử nghiệm n=5–10; Pha B = kit + 2 diễn tập; Pha D = E06, E07, E08, E10 đủ 12, E11 đủ 200/50 người, pilot 30, E12 prod + dự phòng, APK cuối cùng. Rà phạm vi ở 22/9 và 25/9: nếu 22/9 chưa có v0.1 thì bỏ …
- **S7** [major] (timeline-risk) Điểm nghẽn đơn: GPU thuê, máy dev không GPU, hạn mức Claude — Kế hoạch dựa hoàn toàn vào vLLM trên một máy GPU 24 GB thuê ngoài, trong khi máy dev là Windows 11 không GPU: cổng `make doctor` ở E01 yêu cầu 'Docker + GPU nhìn thấy nhau' (plan.md dòng 146) không thể xanh tại chỗ; môi trường dev 'model 1.7B' (dòng 279) không có runtime nào trong stack cho phép (không llama.cpp/Ollama, dòng 481–483) và 'Qwen3.5-1.7B' không tồn tại (xuất hiện ở idea 151/166/270, plan 252/269/279/486,… → Ngày 18/9: đặt thuê GPU ngay (kiểm tra SSH + nvidia-smi trước 20/9) và ghi vào CLAUDE.md/config/models.yaml một đường CPU chính thức: llama.cpp server (hoặc Ollama) với Qwen3.5-4B/2B GGUF cho dev, demo dự phòng, hackathon và fallback chung kết; thêm vào danh sách thư viện cho phép qua ADR-002; thay mọi '1.7B' bằng '2B/4B'. Trước 11/10 chỉ dùng GPU cho phục vụ (~60–80 giờ), để cửa sổ fine-tune ở Pha D. Về Claude: kiểm tra gói/hạn mức trước D1, mặc định effort high cho subagent lặp lại, max chỉ cho architect/security…
- **S8** [major] (timeline-risk) Năng lực con người: một người làm tất cả, lịch thi tháng 10–11 — Bảng phân vai đặt gần như toàn bộ việc lên 'bạn' (duyệt, test mỗi epic, quyết định, duyệt 30 mẫu dữ liệu/vòng, dẫn pilot, hồ sơ, nộp), hai thành viên còn lại không có vai trò kỹ thuật cho tới pilot/video; trong 12 ngày tới cùng lúc phải làm giấy tờ trường, thuê hạ tầng, 6–7 epic, thử nghiệm người thật, 2 video, hồ sơ 20 trang, Drive — vượt xa 'Claude làm 2–5 giờ, bạn làm việc khác'. Hackathon 2 ngày cần 3 người thao … → Phân công lại ngay trong plan §3: Thành viên 2 = giấy tờ trường, giấy xác nhận SV, đối tác pilot, phiếu đồng thuận, sản xuất 2 video (kịch bản từ 25/9, quay 26–27/9, dựng 28/9); Thành viên 3 = viết/soát 13 mục hồ sơ theo MẪU 3 từ 25/9, dẫn thử nghiệm sơ bộ, Google Drive, đối chiếu số liệu; cả hai cài Claude Code và mỗi người 'sở hữu' một nhánh kit ở Pha B. Thu lịch thi/học của cả 3 người ngày 18/9, vẽ lên bảng mốc; đặt 2 buổi pilot chính thức và tuần đóng băng 17–19/11 ngoài ngày thi; nếu một người vắng ngày 10–11/…
- **S9** [major] (timeline-risk) Cổng make check quá nặng cho sprint 12 ngày — `make check` (plan.md dòng 15, 106–117, 258, 372; prompt.md dòng 331) bắt buộc `make eval --quick` 100 mẫu dùng LLM-judge và e2e 3 viewport + docker build chạy trên MỌI PR và trong CI, nhưng không tài liệu nào nói CI lấy endpoint model ở đâu (prompt.md 273 chỉ record/replay cho test, trừ tầng eval); E01-brief D13 chỉ thêm `QUICK=1` cho máy dev và vẫn giữ "CI luôn chạy đầy đủ", nên CI hoặc phải gọi máy GPU thuê (secre… → Chia hai tầng và ghi vào plan §3 + CLAUDE.md: `make check` (< 5 phút, chạy mỗi PR và trong CI): ruff/eslint, unit, contract API, test schema config, test bất biến, gitleaks; `make check-full` (chạy hằng đêm trên máy GPU và trước mỗi tag): e2e 3 viewport, eval quick trên staging, redteam, build, audit. CI không gọi model; nếu thiếu endpoint thì eval bị bỏ qua có ghi rõ. Giữ 'không nới ngưỡng/không xóa test' nhưng bỏ điều kiện 'eval quick xanh' khỏi cổng PR trong Pha A.
- **S10** [major] (timeline-risk) Hai quy trình E01 khác nhau; hook promptlog phải có trước phiên đầu — plan.md §12 P0 yêu cầu Claude Code TẠO hook promptlog trong E01 (nên chính phiên E01 — phiên lớn nhất — không được ghi), còn prompt.md §1/§9 yêu cầu chép `.claude/` + hooks trước phiên đầu và dùng Master Prompt §4 với nội dung E01 khác (5 ADR, subagent, kiểm tra hook). Hai prompt E01 song song với hai CLAUDE.md khác nhau (plan Phụ lục A vs prompt.md 'Delegation') sẽ khiến Claude Code hỏi lại hoặc sinh khung lệch. Vì … → Chỉ định prompt.md §4 là prompt E01 duy nhất; đánh dấu plan.md §12 P0 và Phụ lục A là 'đã thay thế bởi prompt.md' (hoặc xóa) để tránh hai nguồn. Thứ tự ngày 18/9: commit docs/ → chép `.claude/settings.json` + hooks (bản Python cho Windows) → chạy thử hook Stop bằng một phiên rỗng và kiểm tra file xuất hiện trong docs/prompt-log/ → mới chạy Master Prompt. Lưu cả transcript của phiên rà soát hôm nay (nếu chạy qua Claude Code) vào docs/prompt-log/ để chuỗi minh chứng không có khoảng trống.
- **S11** [minor] (timeline-risk) Thiếu cổng quyết định sau vòng khu vực và trình tự chi tiêu — Kế hoạch thiếu cổng quyết định sau kết quả vòng khu vực và chưa xác minh với BTC liệu hồ sơ 30/9 có được cập nhật cho vòng "kiểm tra hồ sơ" chung kết; ngoài ra quy trình đóng băng 48 giờ + máy dự phòng (plan.md dòng 289–292) áp cả cho v1.0 dù BTC chỉ yêu cầu 48 giờ ở chung kết. Tuy nhiên KHÔNG nên hoãn E06–E08/E10/E11/pilot sau 10/10 như đề xuất: các hạng mục này là nội dung bắt buộc của mục 6–9 mẫu hồ sơ nộp trước 3… → Thêm 'Pha C — cổng quyết định 12–18/10' vào plan §2: chỉ khởi động Pha D (E06–E08, E10 đủ, E11 đủ, pilot 30, máy dự phòng, APK) khi có kết quả vào chung kết; trước 11/10 giới hạn GPU ở ~60–80 giờ phục vụ. Gửi câu hỏi cho BTC trước 20/10: có được nộp bản hồ sơ cập nhật cho chung kết không, và sản phẩm 48 giờ được xác minh theo URL nào. Nếu không được cập nhật, mục 12 của hồ sơ 30/9 phải viết đủ rõ lộ trình để bản chung kết vẫn 'khớp hồ sơ'.

## Khoảng trống và xung đột (completeness critic)

- **N01** [blocker] Nguồn sự thật thứ tư: docs/decisions/E01-brief.md tái mã hóa cấu hình đã bị bác bỏ — E01-brief.md (sinh ngày 18/9, tự xưng 'quyết định cho E01' và là file Master Prompt/P0 sẽ coi như đã chốt) giữ nguyên ít nhất 6 điểm mà các lens đã bác: alias `fable` cho subagent, `background: true` cho qa-tester, `isolation: worktree` khi repo chưa có commit, `permissionMode: plan` cho architect phải viết ADR, giữ Qwen3-VL-8B, 'CI luôn chạy đầy đủ'; thêm vào đó protect-paths … → Trước P0/Master Prompt: phát hành E01-brief v2 đối chiếu với toàn bộ CONFIRMED findings (đổi `model: fable` → alias hợp lệ hoặc ID đầy đủ `claude-fable-5-1`; bỏ background/worktree ở E01; architect không `permissionMode: plan` hoặc chuyển việc ghi ADR cho docs-writer; D9 → quyết định ngay theo C03; D13 → theo C04; thêm bảo vệ `.claude/**`). Bổ sung vào quy tắc ưu tiên (CLAUDE.md L463, E01-brief L3) dòng 'E01-brief v2 thắng ba file gốc ở những điểm nó liệt kê rõ; ngoài ra ba f…
- **N02** [blocker] Hạn nộp 6 thành phần cho tuyến trường và cơ chế upload chưa được xác minh — Mọi finding về lịch đều giả định 30/9 là hạn nộp hồ sơ sơ bộ cho đội tuyến trường, nhưng nguồn BTC chỉ nói trường 'gửi danh sách đội' trước 30/9; ai upload 6 thành phần lên hệ thống (đội hay trường), tài khoản đăng ký trên cổng có bắt buộc với đội tuyến trường không, và hạn upload thực tế (có thể sớm hơn theo hạn nội bộ của trường, hoặc muộn hơn nếu BTC mở cổng cho đội được đề … → Ngày 18–19/9 gọi/email BTC (Trung tâm PTKHCN&TNT) và Đoàn trường/Phòng CTSV với 4 câu: (1) đội tuyến trường có phải tự tạo tài khoản và upload 6 thành phần không, hạn nào; (2) trường cần những gì để xét nội bộ và hạn nội bộ; (3) hồ sơ có được cập nhật trước vòng 'kiểm tra hồ sơ' chung kết; (4) hackathon: được dùng công cụ AI/internet/máy chủ riêng và có phải nộp prompt log không. Ghi câu trả lời (kèm ngày, người trả lời) vào BTC-2026-yeu-cau.md; chỉ lập lại lịch sau khi có câ…
- **N03** [major] Staging HTTPS cần từ D5 (E04) nhưng chỉ được dựng ở E12 (D15–16) — Ghi âm trên điện thoại (getUserMedia) đòi secure context nên checklist E04 'thử giọng bạn và người nhà' và tiêu chí E05 'p95 trên staging' cần một URL HTTPS công khai trỏ tới máy GPU từ D5, nhưng deploy chỉ chạy 'từ CI khi tag' (tag đầu v0.1 ở D7), Caddy/monitoring nằm ở E12 (D15–16) và không epic hay gói việc nào của E01-brief giao 'dựng staging'. Đây là lỗ hổng trình tự độc l… → Thêm vào E01 (gói I) deliverable `make deploy ENV=staging` tối thiểu: compose prod-lite + Caddy + `staging.<domain>` trên máy GPU thuê, hoặc `cloudflared`/tunnel tạm từ D2 nếu domain chưa có; DoD E04 phải gồm 'ghi âm được trên Android Chrome qua HTTPS'. Ghi rõ staging là điều kiện tiên quyết của E04/E05 trong bảng phụ thuộc plan.md L222.
- **N04** [major] Kênh phân phối qua Zalo: webview trong app Zalo không đảm bảo micro và cài PWA — Sản phẩm lấy giọng nói làm kênh mặc định và dự kiến người dân mở từ link/QR trong Zalo, nhưng trình duyệt nhúng của Zalo (webview) thường không cấp quyền micro/camera cho trang web và không có 'Thêm vào màn hình chính', nên trải nghiệm cốt lõi (nói–nghe, kèm cặp bằng camera, PWA cài được) có thể hỏng ngay ở pilot và video demo. Không finding nào đề cập rủi ro này. → E02/E04: QR trỏ tới link mở bằng Chrome (Android intent) kèm màn hình chữ to 'Bấm ⋮ → Mở bằng Chrome' khi phát hiện webview Zalo; giữ bàn phím chữ to làm fallback; thêm ca test 'mở từ Zalo' vào checklist E04 và ghi rủi ro vào idea §14. Với bài ôn qua Zalo, gửi link thay vì nhúng.
- **N05** [major] E06/E07 phụ thuộc giả vào E05 — tách ra để mua 3–4 ngày cho mục 6/9 MẪU 3 — Chuỗi E05 → E06 → E07 là phụ thuộc giả: sinh dữ liệu SFT/DPO dùng LLM API đóng vai persona và 'huấn luyện viên mẫu' chỉ cần kịch bản JSON (E02), prompt coach (config/prompts từ E01) và bộ lọc phong cách (guardrails có từ E01), không cần agent E05; fine-tune chỉ cần E06 và GPU. Vì E07 là epic rủi ro nhất (OOM, PEFT chưa kiểm chứng) và là nguồn của mục 6, 8, 9 trong hồ sơ, việc đ… → E06 chạy D3–D4 song song E03/E04 (chỉ tốn API, không cần GPU, chỉ cần 4 kịch bản E02 + bản nháp 2 kịch bản nữa); E07 chạy đêm D5–D7 trên GPU thứ hai thuê 24–48 giờ (~vài trăm nghìn đồng) để không tranh máy với staging; kết quả ablation có từ D8, còn ~5 ngày đệm và một lần chạy lại nếu OOM (dùng QLoRA/4B theo finding fine-tune). Cập nhật plan.md L222 và bảng L205–220.
- **N06** [major] Quyết định ngày 1 chưa được nêu: giữ hay hạ mô-đun 'kèm cặp trên app thật' cho bản nộp — Hai finding đã xác nhận có khuyến nghị đối nghịch về cùng một mô-đun: finding Vision đòi thêm OCR + tập 200 ảnh app thật có nhãn + đo recall che PII (thêm thư viện, thêm thu thập ảnh nhạy cảm), còn finding Privacy đòi không đưa ảnh app thật của thành viên vào repo và coi đó là dữ liệu nhạy cảm. Không finding nào biến nó thành quyết định phạm vi phải chốt trước E01, dù nó quyết … → Chốt ngày 1: bản nộp 30/9 chỉ 'kèm cặp trên ảnh chụp màn hình sandbox/ứng dụng mô phỏng' (VLM đọc ảnh sandbox, không PII, không OCR); 'kèm cặp trên app thật' chuyển sang mục 12 Hướng phát triển kèm ADR nêu điều kiện (OCR mã nguồn mở, tập ảnh có đồng thuận, chỉ số recall che PII ≥ 0,99). Nếu đội vẫn giữ: thêm RapidOCR/PaddleOCR vào allow-list, phiếu đồng thuận riêng cho ảnh thành viên, và dời E08 sau 30/9.
- **N07** [major] Allow-list thư viện trong CLAUDE.md quá hẹp so với chính các khuyến nghị đã xác nhận — sẽ kích hoạt 'dừng và hỏi' liên tục — Quy tắc 'thư viện ngoài danh sách: hỏi trước, ghi ADR' là điểm dừng bắt buộc, nhưng danh sách ở Phụ lục A thiếu gần như mọi thứ mà plan/prompt và các finding đã yêu cầu (python-docx, LibreOffice, k6/Locust, testcontainers, hatchling, jsonschema, onnxruntime, axe/Lighthouse, llama.cpp/Ollama cho đường CPU, OCR nếu giữ N06, gitleaks, pip-audit). Hệ quả: mỗi epic sẽ dừng hỏi nhiều… → E01 tạo `config/allowed-deps.yaml` (tên gói, giấy phép, lý do, epic) làm nguồn duy nhất; `make audit` kiểm lockfile ⊆ danh sách và giấy phép tương thích Apache-2.0; CLAUDE.md chỉ tham chiếu file này. Người dùng duyệt danh sách một lần ở PR E01 thay vì trả lời từng câu hỏi.
- **N08** [major] Đội hình đóng băng sau đăng ký: chưa có bước xác nhận cam kết, tuổi và cùng trường của 3 thành viên trước khi gửi danh sách — BTC cố định thành phần đội sau đăng ký, yêu cầu 12–22 tuổi và cùng trường với tuyến trường, còn MẪU 3 đòi ngày sinh/lớp/khoa của từng người; trong khi idea/plan xếp 'lịch thi trùng' ở xác suất Cao và hai thành viên còn lại không có vai trò kỹ thuật. Không finding nào đặt quyết định 'ai trong đội' (2 hay 3 người, ai cam kết có mặt 10–11/10 và 20–22/11) thành việc ngày 1, dù sau … → Ngày 18/9: bảng 3 dòng (họ tên, ngày sinh, lớp/khoa, cam kết có mặt 10–11/10 và 20–22/11, lịch thi tháng 10–11) ký xác nhận; chỉ đưa vào danh sách người đã cam kết (MẪU 3 cho phép 1–3 thí sinh); giao mỗi thành viên một mô-đun để có thể phản biện (xem N10).
- **N09** [major] Thông tin cá nhân của thành viên (ngày sinh, địa chỉ, điện thoại, email) sẽ vào repo public qua docs/dossier/src/00-thong-tin-doi.md — Khối thông tin thí sinh của MẪU 3 bắt buộc có ngày sinh, xã/phường, điện thoại, email; E01-brief đặt nó ở `docs/dossier/src/00-thong-tin-doi.md` trong repo sẽ bật public khi nộp, và không có deny/protect/gitleaks rule nào cho loại dữ liệu này. Finding bảo mật chỉ nói về prompt log/PII người dùng, chưa nói về PII của chính đội. → Đặt thông tin đội ở `docs/dossier/private/team.yaml` (gitignore) hoặc `.env.dossier`; build_dossier.py đọc từ đó; thêm rule gitleaks tùy chỉnh cho số điện thoại VN/email trong `docs/**`; thư mục Google Drive chỉ chứa PDF (đã có thông tin) chứ không chứa file nguồn này.
- **N10** [major] Không có hoạt động nào để 3 thành viên hiểu hệ thống đủ để phản biện và làm thử thách cải tiến 12 giờ — Chung kết chấm 'demo và phản biện' và 'thử thách cải tiến 12 giờ', tiêu chí có 'mức độ làm chủ mô hình – công cụ – kiến trúc'; nhưng mô hình vận hành đặt con người ở mức 'duyệt, test, góp ý ≤ 30 phút/epic', orchestrator/subagent viết toàn bộ mã, và bản kê khai ghi Claude Code 'Viết mã, tài liệu'. Không finding nào nêu rủi ro 'đội không giải thích được mã của mình' hay cách trìn… → Mỗi PR epic kèm 'Giải thích cho người' ≤ 1 trang (kiến trúc, lý do, cách sửa/đổi, câu hỏi phản biện dự kiến) do docs-writer sinh và thành viên phụ trách mô-đun đọc-ký; mỗi tuần 1 giờ walkthrough 3 người; định nghĩa rõ 'phần đội tự xây' = quyết định thiết kế (ADR), nội dung kịch bản/sư phạm, thiết kế eval, dữ liệu duyệt tay, pilot; diễn tập 12 giờ có người điều khiển Claude Code theo lượt.
- **N11** [major] Bộ 'luật cứng' (hook, settings, CLAUDE.md, CI) không tự bảo vệ, và không có lối bật bảo vệ sau khi E01 tạo ra chúng — prompt.md coi hook là luật mô hình không thể bỏ qua, nhưng deny list và protect-paths không chặn Edit/Write vào `.claude/settings.json`, `.claude/hooks/*`, `CLAUDE.md`, `.github/workflows/**`, `.pre-commit-config.yaml`, và `Bash(git commit *)` cho phép `--no-verify`; lời dặn 'không sửa nếu chưa có ADR' chỉ là prompt. Đồng thời E01 (gói A) bắt buộc phải tạo chính các file này, n… → protect-paths v2: chặn `.claude/**`, `CLAUDE.md`, `.github/workflows/**`, `.pre-commit-config.yaml`, `Makefile` trừ khi file `.claude/BOOTSTRAP` tồn tại (người dùng tạo tay cho phiên E01, xóa khi merge); guard.py chặn `git commit --no-verify`, `git -c core.hooksPath`, `git config core.hooksPath`; CI kiểm sha256 của `.claude/hooks/*` so với `docs/status/hooks.lock` cập nhật bằng PR có nhãn.
- **N12** [minor] `make check` đòi 'prompt log của phiên đã ghi' nhưng log chỉ được ghi ở Stop → lần chạy đầu trong phiên và CI luôn đỏ ở mục này — Cổng docs-check kiểm 'prompt log của phiên' và DoD đòi 'đã ghi và có trong INDEX', trong khi promptlog.py chỉ chạy ở Stop/SessionEnd (sau khi Claude kết thúc lượt); khi Claude chạy `make check` giữa lượt, log phiên hiện tại chưa tồn tại, và trong CI không có phiên nào. Mục này sẽ đỏ giả và cám dỗ 'sửa cho xanh'. → docs-check chỉ kiểm INDEX.md hợp lệ và không file nào lệch hash; điều kiện 'phiên hiện tại đã có log' chuyển sang hook SessionEnd/pre-push và checklist PR; CI bỏ qua mục này.
- **N13** [minor] PostToolUse format.py chạy `ruff --fix` + pytest sau MỖI Edit/Write của 6 subagent đồng thời — Hook format chạy `ruff check --fix`, `prettier --write` và `pytest -q -x` (≤ 60 s) sau mỗi lần sửa file; với fan-out 6 subagent đó là hàng trăm lần pytest/giờ, và `--fix` làm nội dung file lệch với bản Claude vừa viết (Edit tiếp theo trượt `old_string`). Chưa finding nào xét chi phí/độ ổn định của hook này. → PostToolUse chỉ `ruff format`/prettier (không `--fix`, không test); test chạy ở `make check QUICK=1` và pre-commit; nếu muốn phản hồi nhanh, chỉ chạy pytest cho file test vừa sửa.
- **N14** [minor] Prompt Log cho công cụ AI ngoài Claude Code (API sinh dữ liệu, LLM-judge, phiên soạn 3 tài liệu) và cách giải trình 'System Prompt' — Định nghĩa Prompt Log của BTC là lịch sử câu lệnh với 'công cụ AI' nói chung, nhưng kế hoạch chỉ log Claude Code; prompt sinh 8.000 hội thoại/2.000 cặp DPO qua API thương mại, prompt LLM-judge, và các phiên AI đã soạn idea/plan/prompt (ngoài Claude Code) không có nơi lưu. Ngoài ra system prompt nội bộ của Claude Code không xuất được, nên cần giải trình sẵn để tránh bị coi là th… → Thêm `docs/prompt-log/tools/` lưu prompt template + tham số + mẫu đầu ra của bước synth/judge (không cần 8.000 bản); xuất/in PDF các phiên đã soạn 3 tài liệu và đưa vào Drive; README trong Drive giải thích các tầng system prompt (CLAUDE.md + agents + skills + settings snapshot theo git hash; phần nội bộ của Claude Code do Anthropic quản lý).
- **N15** [minor] Quy tắc trung thực 'ảnh thật của bản v1.0' mâu thuẫn với việc nộp trước D21 — cần định nghĩa lại tag — Cả plan.md và prompt.md bắt mọi ảnh trong hồ sơ là 'ảnh thật của bản v1.0', nhưng v1.0 được định nghĩa là D21 trong khi hồ sơ phải nộp quanh D13; nếu không đổi định nghĩa tag, hoặc quy tắc trung thực bị vi phạm hoặc `make dossier` không có bản nào để chụp. → Định nghĩa lại: v1.0 = bản nộp tuyến trường (theo hạn N02), v1.5 = bản khu vực (trước 10/10), v2.0 = bản chung kết; quy tắc = 'ảnh thật của bản được tag nộp', và hồ sơ ghi rõ trạng thái tại ngày nộp + lộ trình tới chung kết.
- **N16** [minor] Các hạng mục có lead time không được lên lịch: giấy xác nhận SV, domain .vn, GPU thuê, branch protection trên GitHub Free, 'máy chủ trong nước' — Ngày 0 liệt kê việc nhưng không tính thời gian chờ: giấy xác nhận sinh viên thường 3–7 ngày làm việc; đăng ký domain .vn cần giấy tờ định danh và 1–2 ngày; A10G trên AWS cần xin quota (không tức thời) và không có vùng Việt Nam; branch protection/rulesets không có cho repo private trên gói GitHub Free (cần Pro qua Student Pack hoặc repo public), nên cổng 'CI xanh mới merge' ở pl… → 18/9: nộp đơn xin giấy xác nhận SV cho cả 3; kích hoạt GitHub Student Pack (Pro) hoặc tạo org; chọn GPU thuê cấp ngay (RunPod/Vast/TensorDock hoặc nhà cung cấp VN như FPT/Viettel/GreenNode nếu muốn giữ cam kết 'trong nước', nếu không thì sửa câu ở idea §9/plan §9); dùng subdomain của domain sẵn có nếu .vn chưa kịp.
- **C01** [major] XUNG ĐỘT khuyến nghị: thời điểm pilot (ba finding, ba lịch khác nhau) — timeline-risk 'Pilot 30 người trước hạn' đề nghị tách pilot khỏi hồ sơ, dùng n=5–10 trước hạn và pilot chính thức trước chung kết; internal-consistency 'Ngưỡng và thiết kế pilot' đề nghị dời buổi 1 lên D14–D15 để giữ thiết kế 2 tuần; timeline-risk minor 'Thiếu cổng quyết định' lại nói KHÔNG hoãn pilot sau 10/10. Ba khuyến nghị không thể cùng thực hiện. → Giải quyết bằng pilot hai tầng: (1) pilot sơ bộ n=5–10 người thân/hàng xóm 50+ có đồng thuận, 1 buổi + ôn ngày 2, trước hạn nộp, ghi rõ quy mô trong mục 8 và không suy diễn thống kê; (2) pilot chính thức 30 người với Đoàn phường, 2 buổi cách 3–5 ngày + ôn ngày 2/5/12, trong khoảng 19/10–10/11 (sau vòng khu vực, trước đóng băng chung kết) để phục vụ 'xác minh sản phẩm' và hồ sơ cập nhật (nếu BTC cho phép, xem N02). Sửa DoD #7 thành hai mốc và bỏ Pilot khỏi chuỗi tiền điều kiện…
- **C02** [major] XUNG ĐỘT khuyến nghị: cắt APK (feasibility) vs đổi tên Qwen3.5-1.7B→2B ở 9 chỗ (3 finding) vs các phụ thuộc còn lại của APK — Finding APK đề nghị thay APK bằng PWA offline ngay, trong khi ba finding khác đề nghị sửa tên model nhỏ thành Qwen3.5-2B ở 9 vị trí và E01-brief D8 đã chốt planner_small; nếu cắt APK thì planner_small, asr_small (PhoWhisper-tiny), bước GGUF, hàng ablation '9B vs bản nhỏ', `apps/android/`, DoD E12 'APK chạy 6 kịch bản', và phương án pilot 'không mạng → APK' đều thành mã chết hoặ… → Quyết định ngày 1 (không đợi D17): cắt APK khỏi phạm vi nộp → xóa planner_small/asr_small/GGUF/hàng ablation/apps/android khỏi config, Makefile, epic, E01-brief; giữ Qwen3.5-2B chỉ như ghi chú 'hướng phát triển'. PWA offline = chế độ bấm + chữ to với clip TTS sinh sẵn cho `coach_line` (tập hữu hạn từ JSON), không ASR; thêm 'kiểm tra kết nối địa điểm' vào checklist pilot-kit thay cho phương án APK.
- **C03** [major] XUNG ĐỘT khuyến nghị: bỏ Qwen3-VL-8B (VRAM) vs giữ và 'chỉ đề xuất' (E01-brief D9) vs LoRA planner text-only vs ablation 'VLM đơn vs YOLOX+VLM' — Finding VRAM/serving kết luận VL-8B thừa và 'VLM tải theo yêu cầu' không khả thi; E01-brief D9 lại giữ VL-8B và chỉ mở câu hỏi vì 'đổi model = điểm dừng'; nhưng nếu gộp thì LoRA huấn luyện trên hội thoại text sẽ áp lên cả lượt đọc ảnh, và hỗ trợ vLLM cho Qwen3.5 (AWQ + LoRA + vision cùng lúc) chưa được kiểm chứng bởi finding nào. Đây là quyết định ảnh hưởng compose, models.yaml… → Ngày 1–2 chạy spike 2 giờ trên máy GPU: vLLM phục vụ một Qwen3.5-9B (AWQ) với multi-LoRA (adapter cho lượt coach, base cho lượt đọc ảnh) và kiểm tra vision input; nếu đạt → ADR-002 chốt 'một model', giữ ablation 'VLM đơn vs YOLOX+VLM' trên cùng model; nếu vLLM chưa hỗ trợ → phương án B là Qwen3.5-4B bf16 làm planner+VLM (không quant) và ghi 'kết quả chưa đạt'. Người dùng chốt ngay tại PR E01, không để câu hỏi mở.
- **C04** [major] XUNG ĐỘT: 'CI luôn chạy đầy đủ' (E01-brief D13, plan §3, branch protection) vs CI GitHub-hosted không có GPU (2 finding) — E01-brief D13 và plan.md giữ eval quick + red-team + e2e + docker build trong cổng PR bắt buộc, còn hai finding (CI/cổng chất lượng; make check quá nặng) kết luận CI không thể chạy phần cần model. Vì E01-brief là 'quyết định cho E01', mâu thuẫn này sẽ khiến CI đỏ vĩnh viễn hoặc phải mở secret tới máy GPU thuê từ PR của mọi worktree. → Tách hai cổng: (a) PR gate trên GitHub-hosted = lint + unit + integration (testcontainers) + e2e record/replay + docker build + audit; (b) `eval-quick`, `redteam` có model, `loadtest` = workflow riêng trên self-hosted runner đặt trên máy GPU, chạy nightly và khi PR gắn nhãn `needs-gpu`; DoD epic ghi rõ epic nào cần (b). Sửa E01-brief D13 và plan.md L106–117 tương ứng.
- **C05** [major] XUNG ĐỘT: 'không hoãn E10/pilot sau 10/10' vs '1–9/10 dành cho bộ kit và diễn tập hackathon' vs 'cắt kịch bản 7–12' vs 'pilot trước chung kết' — timeline-risk minor yêu cầu E06–E08/E10/E11/pilot xong trước hạn hồ sơ và không hoãn sau 10/10; competition-fit/timeline-risk hackathon yêu cầu 9 ngày 1–9/10 cho tách kit + diễn tập 16–20 giờ; scope-cut đề nghị bỏ kịch bản 7–12 (chính là E10); pilot-blocker đề nghị pilot chính thức sau vòng khu vực. Bốn khuyến nghị chưa được hợp nhất thành một lịch. → Lịch ba pha: Pha 1 (đến hạn N02): E01–E05, E06/E07 tách sớm (N05), E09 (10 kịch bản), E10-lite (dashboard cơ bản, tổng 6 kịch bản), E11-lite (red-team 50→100, status page), pilot sơ bộ n=5–10, hồ sơ v1.0 với 13 mục và 'kết quả chưa đạt'. Pha 2 (sau hạn → 9/10): tách plugin/kit, bổ sung đường EDA/bảng/geospatial, diễn tập 16–20 giờ với 3 người, sửa lỗi từ pilot sơ bộ; không mở epic mới. Pha 3 (19/10 → 17/11): E10 đủ 12 kịch bản, E08 (theo N06), E12 prod + máy dự phòng, pilot 3…

## Minor

- CF-08 (competition-fit) — Kịch bản video thuyết trình và demo được viết cho bản v1.0 ngày D21: nhắc kèm cặp VLM (E08, D11), vắc-xin lừa đảo (E09, D12), dashboard (E10, D13), "trang status 48 giờ" (E12, D15–16) và "kết quả pilot và số đo" (D17–19) — đều chưa có vào ngày nộp tuyến trường (≤ D10); docs-writer sẽ sinh script và …
- CF-09 (competition-fit) — Cả ba file khẳng định nội dung thử thách 12 giờ là "tối ưu hệ thống, bảo mật, tích hợp Agent/RAG" và prompt P6//hackathon ưu tiên cứng theo đó, trong khi BTC chỉ công bố "thử thách cải tiến 12 giờ" không kèm nội dung; nếu đề thực tế khác (ví dụ thêm nhóm người dùng, tính năng theo yêu cầu giám khảo,…
- CF-10 (competition-fit) — DoD toàn dự án, prompt P5 và tiêu chí xong của D6 giả định hồ sơ có kết quả pilot ≥ 30 người và bảng ablation fine-tune, với script so khớp số liệu PDF ↔ eval/pilot/report.md; ở ngày nộp tuyến trường chưa có pilot lẫn fine-tune nên `make dossier` sẽ đỏ hoặc mục 8–9 trống, trong khi mẫu vẫn bắt buộc …
- C14 (internal-consistency) — '14 endpoint' nhưng bảng có 15 hàng/16 đường dẫn; 'Sáu prompt' nhưng có P0–P6 = 7; '5 trong 6 được sinh tự động, bạn chỉ quay video và ký' nhưng 2 video + giấy xác nhận = 3/6 cần người; '7 file' cho '6 thành phần'. Danh sách target Makefile khác nhau ở 4 nơi: idea.md 5 lệnh, idea.md 15.1 6 lệnh, CLA…
- C15 (internal-consistency) — settings.json mẫu đặt hai biến môi trường không tồn tại trong Claude Code nên giới hạn '6 subagent đồng thời, 2 tầng' chỉ là lời dặn; `/effort ultracode` không có ở bản 2.1.119 đang cài; §3 nói `.mcp.json` có `postgres` và deny `mcp__*` nhưng §9.1/§9.9 không có; file `mcp.json` rỗng (không dấu chấm)…
- F14 (technical-feasibility) — Kê khai giấy phép có lỗi sự thật và một lỗ hổng: PhoWhisper là BSD-3-Clause chứ không phải Apache-2.0; idea.md dùng 'YOLO' chung chung trong khi plan chốt YOLOX, nên khi bootstrap Claude Code có thể `pip install ultralytics` (AGPL-3.0) và phá giấy phép Apache-2.0 của repo.
- SEC-13 (security-legal-ethics) — Cấu hình MCP không nhất quán và quá rộng: `mcp__github` cho phép toàn bộ tool của server (kể cả push file, merge PR) với PAT lấy từ env; `postgres` MCP được mô tả nhưng không có trong `.mcp.json`; `permissions.deny: ["mcp__*"]` được nói tới nhưng không có trong settings.json mẫu; repo hiện có file `…
- SEC-14 (security-legal-ethics) — Cơ chế bằng chứng prompt log dễ tự tạo dấu hiệu "bị sửa": hook Stop chạy sau mỗi lượt và ghi đè cùng tên file trong cùng phút ⇒ INDEX.md có nhiều hash cho một tên file, và kiểm tra "không file nào bị sửa sau khi ghi" sẽ đỏ với chính hook này; bảo vệ `docs/prompt-log/**` chỉ chặn Edit/Write, Bash vẫn…
- CC-15 (claude-code-config) — Một số chi tiết làm phiên 'Claude chạy 2–5 giờ, bạn làm việc khác' bị gián đoạn hoặc chậm: allow-list thiếu `git merge`, `git push` (deny `--force*` vẫn thắng), `gh`, `npx`, `python`, `ruff`, `prettier`; `disallowedTools: Read(.env)` cho devops dùng sai cú pháp (disallowedTools nhận tên tool) và thừ…
- S12 (timeline-risk) — idea.md mô tả pilot 2 tuần, buổi 2 cách 3–5 ngày, ôn tập cách quãng ngày 2/5/12 qua Zalo; plan.md nén thành D17–D19 (2 buổi cách 2 ngày, 'ôn ở nhà D18 qua Zalo' — kênh Zalo chưa có trong phạm vi v1.0). Khi dời pilot sang Pha D cần chọn một thiết kế.
- S13 (timeline-risk) — Mua domain .vn + trỏ Cloudflare + 2 bản ghi đặt ở ngày 0 nhưng đăng ký .vn cần giấy tờ và 1–3 ngày làm việc; yêu cầu 'Uptime Kuma xanh 24 giờ liên tục' trên staging trước prod và 'status page xanh ≥ 24 giờ' trước nộp cần deploy công khai xong trước 27/9 — chỉ khả thi nếu E12-lite đi trước E09/E10 nh…
