# E01 — Khung repo (bootstrap monorepo, cấu hình Claude Code, tài liệu điều hành)

## Mục tiêu
Dựng toàn bộ khung để 13 epic còn lại chỉ việc chạy `/epic`: uv workspace 10 gói Python + pnpm workspace, `libs/core` dùng chung, `config/*.yaml` có JSON Schema, Makefile với hai cổng `check` (tất định, không model) và `gate` (cần GPU), hook Claude Code bằng Python (prompt log tự ghi từ phiên đầu), CI, Docker Compose profile `cpu`/`gpu`, 14 file epic, 7 ADR, tài liệu vận hành. Kết thúc E01 khi `make check QUICK=1` xanh trên máy dev Windows và PR "[E01] Khung repo" mở với báo cáo reviewer + security-redteam.

## Đầu vào
- `docs/decisions/E01-brief.md` v2 (toàn bộ, đặc biệt §0 D1–D31, §1 cây thư mục, §2 stack, §3–§9 hợp đồng, §10 Makefile, §11 `.claude/`, §15 kiểm thử, §16 phân gói, §17 checklist v2).
- `docs/plan.md` §3 (cổng chất lượng), §5 (kiến trúc, API, schema), §6 hàng E01, §13–§15 (CLAUDE.md, mẫu epic, Makefile) + Errata; `docs/idea.md` §5, §15.1–15.4 + Errata; `docs/prompt.md` §2–§3, §9 + Errata.
- `docs/competition/BTC-2026-yeu-cau.md` (Prompt Log bắt buộc từ phiên đầu; 13 mục MẪU 3); `docs/analysis/2026-09-18-review-findings.md` (70 phát hiện + 21 khoảng trống).
- Không có epic đi trước.

## Công việc
Chia theo gói việc brief §16; mỗi gói chỉ ghi vào thư mục mình sở hữu. Ngoặc vuông là ảnh chụp trạng thái lúc **18/9 11:15** (các gói vẫn chạy song song; số test = số ca `uv run pytest --collect-only` gom được, chưa phải số xanh); gói K cập nhật lại các ngoặc này và hàng E01 của `docs/status/DAILY.md` khi `make check QUICK=1` xanh.

1. **Gói F — nền tảng** [đã có; hai file lạ ở gốc `hd_test.txt`, `hd_test2.txt` không thuộc cây §1 — K xóa trước commit đầu]: `pyproject.toml` (uv workspace 10 member, ruff, pytest `testpaths` gồm mọi gói, coverage 80 % cho `ctcv_core`/`ctcv_agent`/`ctcv_sandbox`/`ctcv_api` ép bởi `make unit`), `uv.lock`, `package.json` + `pnpm-workspace.yaml` (`apps/*`), `.env.example` (khớp cổng `config/app.yaml`, không giá trị thật), `.gitignore` (không bao giờ bỏ qua `docs/prompt-log/`), `.gitattributes`, `.editorconfig`, `.dockerignore`, `.python-version`, `.pre-commit-config.yaml` (ruff, prettier, gitleaks), `.gitleaks.toml`; `libs/core/src/ctcv_core/{config,errors,logging,paths,text,version}.py` + `libs/core/tests/` (6 file test); `config/{app,models,tools,guardrails,eval}.yaml` + `config/schemas/*.schema.json` (10 file: 5 file config + `allowed-deps` + `datasets-registry`/`models-registry` + `scenario`/`drill` do `make schemas` sinh) + `config/prompts/{coach,quarantine,vlm_screen,judge}.v1.md` (header YAML `version/model/purpose`); `tests/conftest.py`, `tests/config/{test_schemas,test_prompts}.py`; `scripts/{doctor,check_docs,check_deps,promptlog_export,declaration,gen_schemas,run_if_exists,write_last_check,evidence,msg}.py` + `scripts/tests/`; `config/allowed-deps.yaml` + `config/schemas/allowed-deps.schema.json`; `Makefile` (36 target = 34 của brief §10 + `docs-check` + `help`, `check` = lint unit integration redteam audit docs-check [+ e2e build khi `QUICK≠1`], `gate` = eval QUICK=1 + redteam-model + loadtest; target chưa hiện thực gọi `scripts/run_if_exists.py` hoặc in "CHUA HIEN THUC"), `README.md` (6 mục docs-check), `LICENSE` Apache-2.0, `SECURITY.md`, `CHANGELOG.md` (`[Unreleased]` có mục `[E01]`); placeholder `pyproject.toml` + `src/<pkg>/__init__.py` cho 9 gói còn lại (`services/api`, `services/agent`, `services/speech`, `services/vision`, `sandbox`, `drills`, `data`, `training`, `eval`).
2. **Gói A — Claude Code**: `.claude/settings.json` (D17, D24: `"model": "claude-fable-5-1"`, `"effortLevel": "xhigh"`, không `CLAUDE_CODE_SUBAGENT_MODEL`, chỉ MCP `playwright`), `.claude/hooks/run.sh` + các file `.py` (session-start, prompt-guard, guard, budget, protect-paths, format, notify, subagent-log, checkpoint, promptlog, daily — fail-closed D22), `.claude/agents/*.md` (10 agent, `model: inherit`, không `background`, D18–D19), `.claude/skills/*/SKILL.md` (16 skill), `.mcp.json`, `CLAUDE.md` ≤ 150 dòng, marker `.claude/BOOTSTRAP`, `plugins/ctcv-kit/` sinh bằng `scripts/sync_plugin.py`. [11:15: `settings.json`, `hooks/` 12 file `.py` + `run.sh` + `tests/` (139 test, xanh), `agents/` 11 file (brief ghi 10 — có thêm `docs-writer`; K kiểm D18–D19), `skills/` 16 thư mục, `.mcp.json`, `CLAUDE.md` 111 dòng, marker `.claude/BOOTSTRAP`, `scripts/sync_plugin.py` đã có; `plugins/ctcv-kit/` chưa sinh — K chạy `make plugin`.]
3. **Gói B — tài liệu điều hành** [file này và các file cùng gói — đã có 33 file; bản 2 lúc 11:15 đồng bộ tên module trong E03–E11/PILOT/DOSSIER với skeleton của gói C/E/G/H/J]: `epics/E01…E12.md`, `epics/PILOT.md`, `epics/DOSSIER.md`; `docs/decisions/ADR-000…ADR-006`, `docs/decisions/BACKLOG.md`; `docs/legal/refs.yaml` + `docs/legal/README.md`; `docs/status/{DAILY,CHECKPOINT}.md`; `docs/prompt-log/{README,INDEX}.md`; `docs/ops/{RUNBOOK,incidents}.md`; `docs/pilot/README.md`; `docs/screens/.gitkeep`; `hackathon/README.md` + `hackathon/input/.gitkeep`.
4. **Gói C — agent**: `services/agent/src/ctcv_agent/{planner,quarantine,router,guardrails,verify}.py`, `services/agent/src/ctcv_agent/tools/` (8 tool, `TOOL_REGISTRY` là `MappingProxyType`, hằng `SIDE_EFFECT`), quarantine trả schema kín (D28), `user_id` lấy từ JWT không phải tham số tool; `services/agent/tests/`; `tests/invariants/` 4 bất biến (whitelist/side effect; không PII; `requires_citation`; taint chuỗi untrusted — §17.7). [11:15: đủ 5 module + `coach.py`, `schemas.py`, `tools/` 8 tool + `base/backends/registry/router.py`, 240 test, README; `tests/invariants/` 4 file.]
5. **Gói D — API**: `services/api/src/ctcv_api/` (FastAPI, prefix `/v1`, 16 route brief §3 — 3 route thật: `GET /v1/scenarios`, `/health`, `/metrics`; còn lại stub 501 `NOT_IMPLEMENTED` tiếng Việt), model SQLAlchemy 9 bảng (D28: `events.payload_json` allowlist theo `type`, `display_name` = biệt danh, `qr_token` có `expires_at`/`revoked_at`), `services/api/alembic/versions/0001_init`, contract test 16 route trong `services/api/tests/` (SQLite in-memory — D15). [11:15: 9 router, `alembic/versions/0001_init.py`, 216 test, README.]
6. **Gói E — sandbox + drills**: `sandbox/src/ctcv_sandbox/{schema,validator,engine}.py`, `sandbox/scenarios/chuyen-khoan-qr.json` (mức 1, qua validator), `sandbox/tests/` (≥ 8 fixture lỗi); `drills/src/ctcv_drills/{schema,validator}.py` (trường `utterance` D27, cấm URL/SĐT/STK/tên thật), `drills/scenarios/gia-danh-cong-an-goi-dien.json`, `drills/tests/`; `make schemas` xuất `config/schemas/{scenario,drill}.schema.json` qua `scripts/gen_schemas.py`. [11:15: sandbox 7 module (`schema/validator/validate/engine/events/loader/normalize`) + 26 fixture lỗi + 136 test + README; drills 7 module (`schema/validator/validate/scoring/loader/rules/normalize`) + `rules.yaml` + 19 fixture lỗi + 120 test + README; hai schema JSON đã sinh.]
7. **Gói G — speech, vision, web**: `services/speech/src/ctcv_speech/`, `services/vision/src/ctcv_vision/` (skeleton + health), `apps/web/` (Vite + React 18 + TS + Tailwind PWA; token cỡ chữ ≥ 20 pt, vùng bấm ≥ 56 px; `apps/web/tests/` vitest; `apps/web/e2e/` smoke chỉ khi `E2E=1`), `apps/android/README.md` (chỉ ghi "hướng phát triển" — ADR-006, F09). [11:15: speech `app/cache/schemas/settings.py` + 27 test + README; vision `app/redact/ttl/schemas/settings.py` + 43 test + README; `apps/web/` src (5 component, i18n, theme, api client) + 7 file vitest + e2e smoke + README; `apps/android/README.md` đã có.]
8. **Gói H — data, training, eval**: `data/src/ctcv_data/pipeline/` skeleton, `data/sources.yaml`, `data/registry/{datasets,models}.yaml` (giấy phép đúng D7/D10; trường `redistribute`, `derived_from`, `source_type`, `tos_checked_on`, `generator_model`, `generator_license`, `tos_url`, `license_url`, `card` — §17.4), `training/budget.json` (`gpu_hours_limit: 20`, `api_vnd_limit: 500000`), `training/configs/`, `eval/redteam/scenarios.jsonl` (50 kịch bản chạy qua `ctcv_agent.guardrails`, không model), `eval/loadtest/k6.js`, `eval/src/ctcv_eval/` runner (`harness`, `redteam`, `loadtest`, `pilot`). [11:15: data 10 bước pipeline (`crawl`, `registry` thật; 8 stub CHƯA HIỆN THỰC) + `sources.yaml` + 2 registry + 46 test + README; training `budget.json`, `configs/{sft,dpo,quantize,yolox,asr_lora}.yaml` + `schema.json`, `budget/configs/runs.py`, 46 test, README; eval đang được ghi: `harness/` (`python -m ctcv_eval.harness`), `suites/` 6 file, `results.py`, `thresholds.py` — còn thiếu `ctcv_eval.redteam` (`suites/redteam.py` import nó), `ctcv_eval.loadtest`, `ctcv_eval.pilot`, `eval/redteam/scenarios.jsonl`, `eval/loadtest/k6.js`, `eval/README.md`, `eval/tests/`; vì thế `make redteam` còn in CHƯA HIỆN THỰC. Tên module của pipeline/eval/drills/vision/speech là nguồn sự thật cho E03–E11.]
9. **Gói I — deploy, CI**: `deploy/docker-compose.yml` (+ `docker-compose.dev.yml`, `docker-compose.prod.yml`; profiles `cpu`/`gpu` — D29; `models` dùng image vLLM upstream, không build), `deploy/docker/<svc>.Dockerfile` (context = gốc repo — D14), `deploy/caddy/Caddyfile`, `deploy/monitoring/`, `deploy/status/`, `deploy/scripts/*.sh`, `.github/workflows/{check,nightly-eval,release}.yml` (CI không bao giờ gọi model — D13; nightly chạy `make gate` trên self-hosted runner ở máy GPU), `.github/PULL_REQUEST_TEMPLATE.md` (4 mục); staging HTTPS tối thiểu (`make deploy ENV=staging`, Caddy hoặc `cloudflared`) là tiên quyết của E04 (N03). [11:15: 4 file compose (thêm `docker-compose.ci.yml`), 5 Dockerfile + `healthcheck.py`/`serve.py`, `caddy/Caddyfile` + `Caddyfile.web`, `monitoring/` (prometheus, alert-rules, alertmanager, grafana, uptime-kuma README), `status/README.md`, 8 script `.sh` (có cả `restore-drill.sh` lẫn `restore_drill.sh` — K giữ một tên; Makefile gọi `restore_drill.sh`), `deploy/tests/` 62 test; `.github/workflows/{check,gate,release}.yml` + README (`gate.yml` giữ vai `nightly-eval.yml` của brief) và `PULL_REQUEST_TEMPLATE.md` đã có.]
10. **Gói J — hồ sơ** [đã có; 15 test; kịch bản video ở `docs/dossier/video/`; verifier 18/9: mục 2 hồ sơ 629 từ > ngân sách 550 + 10 % → gói J cắt còn ≤ 605 từ]: `docs/dossier/src/00-thong-tin-doi.md`, `01-bai-toan.md … 13-lich-su-cau-lenh.md` (H1 = tên nguyên văn 13 mục MẪU 3, placeholder `{{eval:<key>}}` và `{{team:<key>}}`), `docs/dossier/build/build_dossier.py` + `md2docx.py` + `tests/` (python-docx điền `docs/template/AI2026_Mau_ho_so.docx`, chỉ phần MẪU 3; cổng LEG-11 đọc `docs/legal/refs.yaml`; PDF qua LibreOffice), `docs/dossier/figures/`, `docs/dossier/private/team.example.yaml` + `README.md` (D25).
11. **Gói K — tích hợp** (chạy sau cùng): `uv sync`, `pnpm install`, `make check QUICK=1` xanh, rà checklist brief §17 (13 mục) cho mọi gói viết trước v2, `make build`; xóa `hd_test.txt`/`hd_test2.txt` ở gốc; xác nhận mọi gói có `README.md` (hatchling `readme = "README.md"` — thiếu một file là `uv run` gãy toàn repo, đã xảy ra với agent/training); `make plugin`; cập nhật ngoặc vuông của epic này + hàng E01 trong `docs/status/DAILY.md`; xóa marker `.claude/BOOTSTRAP` khi merge E01.
12. Việc của **người dùng** (không phải Claude — brief §17.13): commit đầu tiên `chore(E00): bootstrap docs and Claude Code config` (CC-11), gọi Đoàn trường/Phòng CTSV + BTC (4 câu hỏi N02), chốt 3 thành viên (N08), thuê GPU (SSH + `nvidia-smi` trước 20/9), GitHub Pro/Student Pack cho branch protection, xuất hội thoại đã soạn 3 tài liệu vào `docs/prompt-log/pre-D1/` (N14), duyệt `config/allowed-deps.yaml` một lần (N07), quyết định ADR-002 (D9) và ADR-006.

## Đầu ra
Monorepo theo cây brief §1 (không thư mục ngoài danh sách; mỗi gói Python có `src/` layout, hatchling, và thư mục `tests/` riêng theo `testpaths` của `pyproject.toml`); `uv.lock` duy nhất; `Makefile` với mọi target brief §10 (target chưa hiện thực in "CHƯA HIỆN THỰC — epic EXX" và chỉ exit 0 khi không nằm trong `check`); `.claude/` đầy đủ + `plugins/ctcv-kit/`; CI xanh trên PR đầu; 14 epic; ADR-000…006 + BACKLOG; `docs/status/last_check.json` ghi bởi `make check` (`scripts/write_last_check.py`); prompt log của chính phiên E01 có trong `docs/prompt-log/INDEX.md` sau SessionEnd; PR "[E01] Khung repo" kèm báo cáo reviewer và security-redteam.

## Test nghiệm thu (tự động, phải xanh)
- `make doctor` — exit 0 trên máy dev Windows (GPU/DNS chỉ là INFO; `scripts/doctor.py` chỉ FAIL khi thiếu uv, Python 3.12, 3 file nguồn sự thật, template docx); `make doctor GPU=1` chỉ khi có máy thuê.
- `make check QUICK=1` — lint + unit + integration + redteam (guardrail thuần) + audit + docs-check; xanh trên máy dev; `make check` đầy đủ (thêm e2e, build) xanh trong CI GitHub-hosted, không gọi model.
- `uv run pytest` từ gốc: mọi gói xanh; `make unit` ép coverage ≥ 80 % cho `ctcv_core`, `ctcv_agent`, `ctcv_sandbox`, `ctcv_api`.
- `uv run pytest tests/config` — 5 file yaml qua schema; `models.yaml` chỉ chứa model trong danh sách cho phép; `tools.yaml` == `TOOL_REGISTRY`; `eval.yaml` không có `block` chặt hơn `accept`; 4 prompt có header hợp lệ.
- `uv run pytest tests/invariants` — (a) tool whitelist + side effect ⊆ {none, sandbox, log}, không module tool nào import `httpx`/`requests`/`subprocess`; (b) không PII trong fixture/log mẫu, model DB không cột CCCD/OTP/password; (c) `requires_citation` bắt câu dữ kiện không nguồn; (d) không chuỗi tự do từ nguồn untrusted vào prompt planner.
- `uv run pytest sandbox` — validator từ chối ≥ 8 kịch bản lỗi; engine đi trọn `chuyen-khoan-qr`. `uv run pytest drills` — validator từ chối kịch bản có lời thoại hoàn chỉnh, URL, SĐT, STK.
- `uv run pytest services/api` — contract 16 route; `/v1/scenarios` trả kịch bản mẫu; `/health` 200 với `checks` "skipped" khi chưa cấu hình; stub trả `NOT_IMPLEMENTED` thông điệp tiếng Việt.
- `make redteam` — 50 kịch bản `eval/redteam/scenarios.jsonl` qua guardrail thuần = 0 lỗi.
- `pnpm -C apps/web lint && pnpm -C apps/web test` — token cỡ chữ ≥ 20 pt, vùng bấm ≥ 56 px; `E2E=1 make e2e` smoke trên 3 viewport.
- `uv run python -m pytest .claude/hooks/tests` — guard chặn đúng danh sách D22 (kể cả `rm -fr`, `--no-verify`, `docker compose config`, mọi token `.env`), protect-paths chặn `docs/prompt-log/**` và `.claude/**` khi không có marker, promptlog ghi 1 file/session và 1 dòng INDEX ở SessionEnd, fail-closed khi JSON lỗi.
- `uv run python scripts/check_docs.py` — README đủ 6 mục, CHANGELOG có `[Unreleased]`, `docs/prompt-log/INDEX.md` có mặt.
- `make audit` — gitleaks, pip-audit, pnpm audit sạch; lockfile ⊆ `config/allowed-deps.yaml`; không AGPL/GPL/CC-NC ở runtime; cấm `ultralytics`, `yolov5`.

_Trạng thái lúc 18/9 11:15 (K cập nhật lần cuối): `uv run python scripts/check_docs.py` xanh; `.claude/hooks/tests` 139/139 xanh; `uv run pytest --collect-only` gom ≈ 1.360 ca (chưa chạy trọn); `make redteam` còn in CHƯA HIỆN THỰC vì `ctcv_eval.redteam`/`scenarios.jsonl` chưa có (gói H); `make check QUICK=1` chưa chạy._

## Không được làm
Tạo thư mục ngoài cây brief §1; thêm thư viện ngoài `config/allowed-deps.yaml`; sửa thân `docs/idea.md`/`docs/plan.md`/`docs/prompt.md` (chỉ errata — D31); sửa tay `docs/prompt-log/**`; đưa `eval QUICK=1`/red-team có model/loadtest vào `make check`; dùng `jq` hay hook bash; alias model `fable`; `background: true` cho qa-tester; worktree trước commit đầu; commit thông tin cá nhân thành viên (`docs/dossier/private/` gitignore); tự chốt ADR-002 (D9) hay ADR-006 thay người dùng; hiện thực nghiệp vụ của E02+ trong E01.

## Câu hỏi mở (≤ 3)
1. ADR-002/D9: chốt "một model Qwen3.5-9B cho cả planner lẫn đọc ảnh" (cần spike 2 giờ trên máy GPU: AWQ + multi-LoRA + vision) hay giữ Qwen3-VL-8B riêng (phương án B)?
2. ADR-006: duyệt lịch 3 pha, tuyến trường 30/9, cắt APK → PWA offline, "kèm cặp trên app thật" → hướng phát triển, pilot hai tầng?
3. Tên sản phẩm chính thức và domain (dùng subdomain sẵn có nếu `.vn` chưa kịp — N16)?

## Checklist cho người dùng (≤ 30 phút)
- [ ] Đọc `CLAUDE.md` (≤ 150 dòng) và `docs/decisions/ADR-006-lich-thuc-te-va-tuyen-dang-ky.md`; trả lời 3 câu hỏi mở ngay trong PR.
- [ ] Chạy `make doctor` rồi `make check QUICK=1` trên máy dev; xác nhận `docs/status/last_check.json` xuất hiện.
- [ ] Mở một phiên Claude Code rỗng, gõ một câu, thoát; kiểm tra `docs/prompt-log/sessions/<session_id>.jsonl` và 1 dòng mới trong `docs/prompt-log/INDEX.md`.
- [ ] Duyệt `config/allowed-deps.yaml` một lần (N07) và `data/registry/models.yaml` (PhoWhisper = BSD-3-Clause, YOLOX = Apache-2.0, không Ultralytics).
- [ ] Làm việc ngày 1 của brief §17.13: gọi Đoàn trường/CTSV + BTC (4 câu hỏi N02), chốt đội (N08), thuê GPU, tạo commit đầu, xuất `docs/prompt-log/pre-D1/`.

## Mốc thực tế
18–19/9/2026 (T6–T7) — Pha A theo ADR-006 (đề xuất, chờ chốt). Khớp plan §6 "D1" vì D1 = 18/9; nhưng lịch D-day của plan.md đặt nộp hồ sơ ở D20–D21 ≈ 7–8/10, sau hạn tuyến trường 30/9 (CF-01/S1/F01) nên các epic sau dùng ngày dương của ADR-006 thay cho D-day; plan.md không bị sửa thân (D31), chỉ có errata. Tuyến đội tự do 20/9 (CN) đã loại (S2). Vòng khu vực 10–11/10, chung kết 20–22/11.
