# Changelog

Mọi thay đổi đáng chú ý của CTCV được ghi ở đây. Định dạng theo [Keep a Changelog](https://keepachangelog.com/vi/1.1.0/), phiên bản theo [SemVer](https://semver.org/lang/vi/). Mỗi epic thêm mục dưới `[Unreleased]` với tiền tố `[E0X]`; khi tag, mục được chuyển xuống phiên bản tương ứng.

## [Unreleased]

### Added

- [E01] Khung kho mã (gói việc F — foundation): uv workspace 10 gói (`libs/core` + 9 member), pnpm workspace, `Makefile` với cổng `check` (tất định, không model) và `gate` (cần GPU/staging) theo brief v2 D13, pre-commit (ruff, prettier, gitleaks cục bộ), `.env.example`, LICENSE Apache-2.0, SECURITY.md, README.
- [E01] `ctcv_core`: `load_config()` có kiểm JSON Schema 2020-12 và ghi đè `CTCV_<NAME>_<KEY>`, `AppError` + lớp con 501/404/403/422 với thông điệp tiếng Việt, log JSON có `request_id` và bộ lọc PII, `count_sentences()` tiếng Việt, đường dẫn chuẩn.
- [E01] `config/`: `app.yaml`, `models.yaml` (thêm `quarantine`, `judge`; `vlm` đề xuất gộp vào Qwen3.5-9B — D9 v2, chờ chốt ở ADR-002), `tools.yaml` (8 tool, side effect đóng), `guardrails.yaml`, `eval.yaml` (ngưỡng plan §7), `allowed-deps.yaml` (D26), 4 prompt hệ thống có phiên bản, JSON Schema cho mọi file cấu hình và 2 registry.
- [E01] `scripts/`: `doctor.py` (+`--gpu`), `check_docs.py`, `check_deps.py`, `write_last_check.py`, `run_if_exists.py`, `gen_schemas.py`, `promptlog_export.py` (kiểm hash + redaction gate D21), `declaration.py`, `evidence.py`, cùng test.
- [E01] `tests/config`: mọi YAML qua schema; whitelist model và tool; ngưỡng eval nhất quán; regex guardrail khớp fixture; prompt có front matter.
- [DFL] Dữ liệu (P1): 107 thủ tục hành chính hợp lệ (109 trang, 8 lĩnh vực) thu từ Cổng Dịch vụ công Bộ Công an, chuẩn hóa theo schema `tthc-record` v1; chỉ mục lai BM25 + bge-m3 (955 đoạn).
- [DFL] Engine hỏi đáp (P2b/P3): truy xuất lai, cổng khớp thủ tục, soạn câu bằng `qwen3.5:2b` có kiểm chứng số và từ vựng theo nguồn; không đủ căn cứ thì trả câu mẫu tất định hoặc "không chắc" và mời tình nguyện viên; câu hỏi OTP, nhờ làm thay, nội dung dán vào có câu an toàn cố định.
- [DFL] API (P4): `/v1/coach/ask` chạy thật, thêm `/v1/coach/intake-check` cho cán bộ/tình nguyện viên (tổng 17 route `/v1`); chế độ demo `join`/`login` mặc định tắt, chỉ bật khi đặt biến demo và bị cấm ở prod.
- [DFL] Web (P5): màn hình người dân `#hoi-thu-tuc` (trả lời, nút xanh "Nguồn", thẻ giấy tờ, hộp mời tình nguyện viên) và màn hình cán bộ `#can-bo` (tìm thủ tục, chọn trường hợp, đánh dấu giấy tờ, câu nhắn cho người dân).
- [DFL] Đánh giá (P6): bộ 130 câu sinh từ mẫu và 83 câu soạn tay, chia dev/test tất định; chỉ số trích dẫn, bịa số, độ trung thành số, từ chối đúng, độ trễ.
- [DFL] Lượt đo đầy đủ (P14, 25/9): `eval/reports/latest.json`, `ablation-tthc.json` (3 chế độ trên cùng một lượt gọi mô hình), `calibration-tthc.md`, `env-tthc.json` (máy đo, digest mô hình, ghi rõ Ollama chạy trên GPU RTX 4050 Laptop).
- [DFL] Hồ sơ v1 (P7c): `docs/dossier/out/dfl/de-xuat-giai-phap.pdf` 9 trang dựng tự động từ `eval/reports/` (bản `.docx` để điền đội), `mo-ta-ngan.filled.md`, `cam-ket.docx`, `BAN-GIAO.md`; bảng đội để ô trống cho người dùng điền. Video chưa có (bước quay màn cán bộ dừng vì lỗi giao diện).
- [DFL] Web v2 (P5): tên giấy tờ và nhãn trường hợp dài hiện câu đầu (≤ 120 ký tự) kèm nút "Xem đủ"; giấy tờ chỉ cần khi đúng trường hợp (cờ `conditional` hoặc trạng thái `neu_ap_dung`, tùy chọn) có nhãn riêng và không bị tính là còn thiếu; chân trang "Gọi tình nguyện viên" chuyển sang sticky nên không che cuối trang; `useIntake` và `AskForm` tách còn ≤ 50 dòng; giá trị giả trong test web không còn bị gitleaks bắt nhầm.
- [DFL] `scripts/dev_cpu.py` (P9): dựng demo CPU (API + web, chỉ nghe 127.0.0.1) với Ollama cục bộ, bí mật demo tạm thời không in ra; hướng dẫn `docs/ops/cpu-demo.md`.
- [DFL] Engine v2 (sửa lỗi độ chính xác P8 và red-team, 25/9): kiểm số theo **đơn vị** (đồng/đô la, ngày/tháng/năm; "7 triệu" cần 7.000.000 trong nguồn); ý định mới `han_phai_lam` — hỏi hạn người dân phải làm ("trong bao lâu phải đi trình báo") chỉ đọc mục điều kiện/trình tự, số của mục thời hạn giải quyết bị loại (`number_wrong_role`); phí có "áp dụng đến hết ngày …" đã qua thì không đọc số như mức hiện hành và mời cán bộ; chọn thủ tục ít từ thừa ở đầu tên khi độ trùng bằng nhau ("Cấp hộ chiếu" thay vì "Trình báo mất hộ chiếu", lý lịch tư pháp của công dân thay vì theo yêu cầu cơ quan tố tụng); cổng từ chối việc ngoài phạm vi (`gate.out_of_scope_phrases`: chứng thực, công chứng, kết hôn…); "mần" → "làm"; `serving.num_gpu_env` (`CTCV_OLLAMA_NUM_GPU=0`) để đo độ trễ chỉ-CPU. Thử thật 83 câu soạn tay: 67 → 72 đạt, lỗi nặng/trung bình 5 → 0 (11 câu escalate thừa còn nguyên); 5 bước hỏi của demo C9 và màn cán bộ không đổi.
- [DFL] Đối chiếu hồ sơ v2: giấy tờ mà tên tự nêu trường hợp riêng có trạng thái `neu_ap_dung`, không tính là thiếu; tin nhắn luôn ≤ 2 câu; nhãn trường hợp dài (tới 918 ký tự) được nhận lại; chạy 107 bản ghi × mọi trường hợp: 450 lượt, 0 lỗi (trước 10).
- [DFL] Lượt đo v2 (P14, 25/9): `latest.json` và `ablation-tthc.json` đo lại sau khi sửa engine (Ollama trên GPU RTX 4050 Laptop); thêm `eval/reports/latency-cpu-only.json` và `.md` (cùng mã, cùng tập test, `CTCV_OLLAMA_NUM_GPU=0`, bằng chứng `ollama ps` 100 % CPU) để báo độ trễ chỉ-CPU; bảng so sánh v1 → v2 trong `eval/reports/2026-09-25.md`. Bộ chấm ablation lấy thủ tục mà engine đã định tuyến, không lấy kết quả truy xuất đầu tiên.
- [DFL] Hồ sơ v2 (P7c): `docs/dossier/out/dfl/v2/` (PDF 9 trang đủ 4 hình, `.docx`, `mo-ta-ngan.filled.md`, `cam-ket.docx`, `BAN-GIAO-v2.md`); video v2 `v2/video/ctcv-dfl-2026.mp4` dài 143,3 s, quay lại trên hệ thống thật; bản v1, v1.1 giữ nguyên.
- [DFL] Tài liệu phát hành công khai (repo GitHub làm "link sản phẩm dùng thử"): viết lại `README.md` cho giám khảo (dùng thử 10 phút, 5 câu hỏi mẫu, luồng cán bộ, sơ đồ Mermaid 7 lớp, dữ liệu và giấy phép dữ liệu, bảng số đo v2 có đường dẫn nguồn và ghi rõ "tự chấm → cận trên", cấu hình GPU và chỉ-CPU, giới hạn nói thẳng, English summary); thêm `NOTICE` (bên thứ ba: mô hình, thư viện runtime, dữ liệu © Cổng Dịch vụ công - Bộ Công an không phân phối lại), `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` (Contributor Covenant 2.1), `CITATION.cff`, `docs/DEMO.md` (hướng dẫn dùng thử, xử lý sự cố, chế độ `template_only`). Đo lại số test không cần mô hình: `eval/reports/engineering-2026-09-25.json` (2.491 test Python: 2.484 đạt, 1 đỏ, 6 bỏ qua; web 118/118; bản 19/9 giữ nguyên ở `engineering.json`).

### Changed

- [DOSSIER] Đổi mục tiêu dự thi sang Data for Life 2026, đề DA940-01 (ADR-007, đề xuất — chờ người dùng duyệt; phụ lục `ADR-007-hop-dong.md`). Thêm kế hoạch RAG thủ tục hành chính (dữ liệu Cổng Dịch vụ công Bộ Công an, chỉ mục lai, lớp kiểm chứng). ADR-006 được ghi là thay thế bởi ADR-007. Thêm ADR-008 (đề xuất): lịch vòng 2–4 và phương án cho ba lỗi nền của cổng audit. Thêm nguồn hồ sơ `docs/dossier/dfl/` (mô tả ngắn, checklist nộp, cam kết, thư đề nghị dữ liệu, mẫu thông tin đội).
- [E01] Giấy phép PhoWhisper ghi đúng BSD-3-Clause (D10); bộ phát hiện giao diện là YOLOX-Tiny Apache-2.0 thay cho Ultralytics YOLO (D7); model nhỏ là Qwen3.5-2B (D8) và là đường CPU chính thức (D29).

- [DFL] Bản đề xuất và kịch bản video ghi độ trễ là số đo có GPU hỗ trợ (trước đây ghi nhầm "CPU, không GPU"); mục "Chưa đạt" thêm lỗi màn cán bộ và kết quả yếu ở câu không dấu, khẩu ngữ, phương ngữ.
- [DFL] Bản đề xuất v2: mục "Chưa đạt, nói thẳng" ghi đúng phần đã sửa (mốc "áp dụng đến hết ngày" của phí, giấy tờ có điều kiện ở màn cán bộ, kiểm số có đơn vị) và phần còn thiếu; thêm dòng độ trễ chỉ-CPU; bỏ chú thích (*) của cột answer_accuracy; Hình 1–4 đặt lại sau phần chữ để không còn nửa trang trống. Nguồn: `docs/dossier/dfl/de-xuat-giai-phap.md`, `mo-ta-ngan.md`, `kich-ban-video.md` (thời lượng thật), `README.md`.

### Security

- [E01] Ba nguyên tắc bất biến ghi trong SECURITY.md; gitleaks chạy ở pre-commit và `make audit`; quy tắc phát hiện SĐT/e-mail Việt Nam trong `docs/**` (D25).
- [DFL] SECURITY.md: báo lỗ hổng chỉ qua GitHub Security Advisories (không e-mail cá nhân); ghi phạm vi route đang phục vụ thật của prototype; số kịch bản red-team hiện tại (92); chưa có tag phát hành nên chỉ `main` nhận bản vá.
- [DFL] Red-team v2 (25/9): guardrail bắt câu gõ không dấu ("gui mat khau", "nop giup bac"), đòi mã không nêu tên ("đọc cái mã 6 số vừa về máy"), nhờ làm thay có tân ngữ dài ("gửi đơn … lên cổng … giùm bác"), "bấm vào đường link" không có URL; CCCD/SĐT viết cách nhóm được che trước khi tới model; câu composer nhận đã làm thay hoặc bảo đọc/mang mã bị loại; câu mẫu có link/PII (bản ghi nhiễm) không được đọc; `CTCV_ENV` chuẩn hóa (`production`/`PROD` = prod) và bật demo phải có `JWT_SECRET` riêng; `session_id` ≤ 64 ký tự. 4 kịch bản GP-05/06/08/09 chuyển vào `eval/redteam/scenarios.jsonl` (92/92 đạt).
